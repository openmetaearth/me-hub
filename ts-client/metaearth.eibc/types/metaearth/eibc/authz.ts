// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: metaearth/eibc/authz.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Coin } from "../../cosmos/base/v1beta1/coin";

export const protobufPackage = "metaearth.eibc";

/**
 * DecProto defines a Protobuf wrapper around a Dec object.
 * Deprecated: Prefer to use math.LegacyDec directly. It supports binary Marshal
 * and Unmarshal.
 */
export interface DecProto {
  dec: string;
}

/**
 * FulfillOrderAuthorization allows the grantee to fulfill eIBC demand orders
 * from the granter's account.
 */
export interface FulfillOrderAuthorization {
  /**
   * rollapps is an optional list of rollapp criteria that the grantee can
   * fulfill demand orders from
   */
  rollapps: RollappCriteria[];
}

export interface RollappCriteria {
  /**
   * rollapp_id is the unique identifier of the rollapp that the grantee can
   * fulfill demand orders from
   */
  rollappId: string;
  /**
   * denoms is an optional list of denoms that the grantee can fulfill demand
   * orders for
   */
  denoms: string[];
  /** max_price is the optional maximum order price acceptable to the granter */
  maxPrice: Coin[];
  /**
   * min_fee_percentage is the minimum gross fee earning percentage the LP is
   * willing to get from a demand order
   */
  minFeePercentage:
    | DecProto
    | undefined;
  /**
   * operator_fee_share is the share of the fee earnings willing to give to the
   * operator
   */
  operatorFeeShare:
    | DecProto
    | undefined;
  /**
   * settlement_validated is the flag to only fulfill demand orders that have
   * been settlement validated
   */
  settlementValidated: boolean;
  /**
   * spend_limit is the optional maximum amount of coins that can be spent by
   * the grantee
   */
  spendLimit: Coin[];
}

function createBaseDecProto(): DecProto {
  return { dec: "" };
}

export const DecProto: MessageFns<DecProto> = {
  encode(message: DecProto, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dec !== "") {
      writer.uint32(10).string(message.dec);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DecProto {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDecProto();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.dec = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DecProto {
    return { dec: isSet(object.dec) ? globalThis.String(object.dec) : "" };
  },

  toJSON(message: DecProto): unknown {
    const obj: any = {};
    if (message.dec !== "") {
      obj.dec = message.dec;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DecProto>, I>>(base?: I): DecProto {
    return DecProto.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DecProto>, I>>(object: I): DecProto {
    const message = createBaseDecProto();
    message.dec = object.dec ?? "";
    return message;
  },
};

function createBaseFulfillOrderAuthorization(): FulfillOrderAuthorization {
  return { rollapps: [] };
}

export const FulfillOrderAuthorization: MessageFns<FulfillOrderAuthorization> = {
  encode(message: FulfillOrderAuthorization, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.rollapps) {
      RollappCriteria.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FulfillOrderAuthorization {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseFulfillOrderAuthorization();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.rollapps.push(RollappCriteria.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): FulfillOrderAuthorization {
    return {
      rollapps: globalThis.Array.isArray(object?.rollapps)
        ? object.rollapps.map((e: any) => RollappCriteria.fromJSON(e))
        : [],
    };
  },

  toJSON(message: FulfillOrderAuthorization): unknown {
    const obj: any = {};
    if (message.rollapps?.length) {
      obj.rollapps = message.rollapps.map((e) => RollappCriteria.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FulfillOrderAuthorization>, I>>(base?: I): FulfillOrderAuthorization {
    return FulfillOrderAuthorization.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FulfillOrderAuthorization>, I>>(object: I): FulfillOrderAuthorization {
    const message = createBaseFulfillOrderAuthorization();
    message.rollapps = object.rollapps?.map((e) => RollappCriteria.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRollappCriteria(): RollappCriteria {
  return {
    rollappId: "",
    denoms: [],
    maxPrice: [],
    minFeePercentage: undefined,
    operatorFeeShare: undefined,
    settlementValidated: false,
    spendLimit: [],
  };
}

export const RollappCriteria: MessageFns<RollappCriteria> = {
  encode(message: RollappCriteria, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollappId !== "") {
      writer.uint32(10).string(message.rollappId);
    }
    for (const v of message.denoms) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.maxPrice) {
      Coin.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.minFeePercentage !== undefined) {
      DecProto.encode(message.minFeePercentage, writer.uint32(34).fork()).join();
    }
    if (message.operatorFeeShare !== undefined) {
      DecProto.encode(message.operatorFeeShare, writer.uint32(42).fork()).join();
    }
    if (message.settlementValidated !== false) {
      writer.uint32(48).bool(message.settlementValidated);
    }
    for (const v of message.spendLimit) {
      Coin.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RollappCriteria {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRollappCriteria();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.rollappId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.denoms.push(reader.string());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.maxPrice.push(Coin.decode(reader, reader.uint32()));
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.minFeePercentage = DecProto.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.operatorFeeShare = DecProto.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.settlementValidated = reader.bool();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.spendLimit.push(Coin.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RollappCriteria {
    return {
      rollappId: isSet(object.rollappId)
        ? globalThis.String(object.rollappId)
        : isSet(object.rollapp_id)
        ? globalThis.String(object.rollapp_id)
        : "",
      denoms: globalThis.Array.isArray(object?.denoms) ? object.denoms.map((e: any) => globalThis.String(e)) : [],
      maxPrice: globalThis.Array.isArray(object?.maxPrice)
        ? object.maxPrice.map((e: any) => Coin.fromJSON(e))
        : globalThis.Array.isArray(object?.max_price)
        ? object.max_price.map((e: any) => Coin.fromJSON(e))
        : [],
      minFeePercentage: isSet(object.minFeePercentage)
        ? DecProto.fromJSON(object.minFeePercentage)
        : isSet(object.min_fee_percentage)
        ? DecProto.fromJSON(object.min_fee_percentage)
        : undefined,
      operatorFeeShare: isSet(object.operatorFeeShare)
        ? DecProto.fromJSON(object.operatorFeeShare)
        : isSet(object.operator_fee_share)
        ? DecProto.fromJSON(object.operator_fee_share)
        : undefined,
      settlementValidated: isSet(object.settlementValidated)
        ? globalThis.Boolean(object.settlementValidated)
        : isSet(object.settlement_validated)
        ? globalThis.Boolean(object.settlement_validated)
        : false,
      spendLimit: globalThis.Array.isArray(object?.spendLimit)
        ? object.spendLimit.map((e: any) => Coin.fromJSON(e))
        : globalThis.Array.isArray(object?.spend_limit)
        ? object.spend_limit.map((e: any) => Coin.fromJSON(e))
        : [],
    };
  },

  toJSON(message: RollappCriteria): unknown {
    const obj: any = {};
    if (message.rollappId !== "") {
      obj.rollappId = message.rollappId;
    }
    if (message.denoms?.length) {
      obj.denoms = message.denoms;
    }
    if (message.maxPrice?.length) {
      obj.maxPrice = message.maxPrice.map((e) => Coin.toJSON(e));
    }
    if (message.minFeePercentage !== undefined) {
      obj.minFeePercentage = DecProto.toJSON(message.minFeePercentage);
    }
    if (message.operatorFeeShare !== undefined) {
      obj.operatorFeeShare = DecProto.toJSON(message.operatorFeeShare);
    }
    if (message.settlementValidated !== false) {
      obj.settlementValidated = message.settlementValidated;
    }
    if (message.spendLimit?.length) {
      obj.spendLimit = message.spendLimit.map((e) => Coin.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RollappCriteria>, I>>(base?: I): RollappCriteria {
    return RollappCriteria.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RollappCriteria>, I>>(object: I): RollappCriteria {
    const message = createBaseRollappCriteria();
    message.rollappId = object.rollappId ?? "";
    message.denoms = object.denoms?.map((e) => e) || [];
    message.maxPrice = object.maxPrice?.map((e) => Coin.fromPartial(e)) || [];
    message.minFeePercentage = (object.minFeePercentage !== undefined && object.minFeePercentage !== null)
      ? DecProto.fromPartial(object.minFeePercentage)
      : undefined;
    message.operatorFeeShare = (object.operatorFeeShare !== undefined && object.operatorFeeShare !== null)
      ? DecProto.fromPartial(object.operatorFeeShare)
      : undefined;
    message.settlementValidated = object.settlementValidated ?? false;
    message.spendLimit = object.spendLimit?.map((e) => Coin.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
