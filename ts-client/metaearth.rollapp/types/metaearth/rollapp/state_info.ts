// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: metaearth/rollapp/state_info.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { Status, statusFromJSON, statusToJSON } from "../common/status";
import { BlockDescriptors } from "./block_descriptor";

export const protobufPackage = "metaearth.rollapp";

/**
 * StateInfoIndex is the data used for indexing and retrieving a StateInfo
 * it updated and saved with every UpdateState in StateInfo.
 * We use the this structure also for:
 * 1. LatestStateInfoIndex which defines the rollapps' current (latest) index of
 * the last UpdateState
 * 2. LatestFinalizedStateIndex which defines the rollapps' current (latest)
 * index of the latest StateInfo that was finalized
 */
export interface StateInfoIndex {
  /**
   * rollappId is the rollapp that the sequencer belongs to and asking to update
   * it used to identify the what rollapp a StateInfo belongs
   * The rollappId follows the same standard as cosmos chain_id
   */
  rollappId: string;
  /**
   * index is a sequential increasing number, updating on each
   * state update used for indexing to a specific state info, the first index is
   * 1
   */
  index: number;
}

/** StateInfo defines a rollapps' state. */
export interface StateInfo {
  /**
   * stateInfoIndex defines what rollapp the state belongs to
   * and in which index it can be referenced
   */
  stateInfoIndex:
    | StateInfoIndex
    | undefined;
  /** sequencer is the bech32-encoded address of the sequencer sent the update */
  sequencer: string;
  /** startHeight is the block height of the first block in the batch */
  startHeight: number;
  /** numBlocks is the number of blocks included in this batch update */
  numBlocks: number;
  /** DAPath is the description of the location on the DA layer */
  DAPath: string;
  /** creationHeight is the height at which the UpdateState took place */
  creationHeight: number;
  /** status is the status of the state update */
  status: Status;
  /**
   * BDs is a list of block description objects (one per block)
   * the list must be ordered by height, starting from startHeight to
   * startHeight+numBlocks-1
   */
  BDs:
    | BlockDescriptors
    | undefined;
  /** created_at is the timestamp at which the StateInfo was created */
  createdAt:
    | Date
    | undefined;
  /**
   * NextProposer is the bech32-encoded address of the proposer that we expect
   * to see in the next state info. Most of the time NextProposer is the current
   * proposer. In case of rotation it is changed to the successor.
   */
  nextProposer: string;
}

/** StateInfoSummary is a compact representation of StateInfo */
export interface StateInfoSummary {
  /**
   * stateInfoIndex defines what rollapp the state belongs to
   * and in which index it can be referenced
   */
  stateInfoIndex:
    | StateInfoIndex
    | undefined;
  /** status is the status of the state update */
  status: Status;
  /** creationHeight is the height at which the UpdateState took place */
  creationHeight: number;
}

/**
 * BlockHeightToFinalizationQueue defines a map from block height to list of
 * states to finalized
 */
export interface BlockHeightToFinalizationQueue {
  /** CreationHeight is the block height that the state should be finalized */
  creationHeight: number;
  /**
   * FinalizationQueue is a list of states that are waiting to be finalized
   * when the block height becomes creationHeight
   */
  finalizationQueue: StateInfoIndex[];
  /** RollappID is the rollapp which the queue belongs to */
  rollappId: string;
}

function createBaseStateInfoIndex(): StateInfoIndex {
  return { rollappId: "", index: 0 };
}

export const StateInfoIndex: MessageFns<StateInfoIndex> = {
  encode(message: StateInfoIndex, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollappId !== "") {
      writer.uint32(10).string(message.rollappId);
    }
    if (message.index !== 0) {
      writer.uint32(16).uint64(message.index);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StateInfoIndex {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseStateInfoIndex();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.rollappId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.index = longToNumber(reader.uint64());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): StateInfoIndex {
    return {
      rollappId: isSet(object.rollappId) ? globalThis.String(object.rollappId) : "",
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
    };
  },

  toJSON(message: StateInfoIndex): unknown {
    const obj: any = {};
    if (message.rollappId !== "") {
      obj.rollappId = message.rollappId;
    }
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StateInfoIndex>, I>>(base?: I): StateInfoIndex {
    return StateInfoIndex.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StateInfoIndex>, I>>(object: I): StateInfoIndex {
    const message = createBaseStateInfoIndex();
    message.rollappId = object.rollappId ?? "";
    message.index = object.index ?? 0;
    return message;
  },
};

function createBaseStateInfo(): StateInfo {
  return {
    stateInfoIndex: undefined,
    sequencer: "",
    startHeight: 0,
    numBlocks: 0,
    DAPath: "",
    creationHeight: 0,
    status: 0,
    BDs: undefined,
    createdAt: undefined,
    nextProposer: "",
  };
}

export const StateInfo: MessageFns<StateInfo> = {
  encode(message: StateInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stateInfoIndex !== undefined) {
      StateInfoIndex.encode(message.stateInfoIndex, writer.uint32(10).fork()).join();
    }
    if (message.sequencer !== "") {
      writer.uint32(18).string(message.sequencer);
    }
    if (message.startHeight !== 0) {
      writer.uint32(24).uint64(message.startHeight);
    }
    if (message.numBlocks !== 0) {
      writer.uint32(32).uint64(message.numBlocks);
    }
    if (message.DAPath !== "") {
      writer.uint32(42).string(message.DAPath);
    }
    if (message.creationHeight !== 0) {
      writer.uint32(56).uint64(message.creationHeight);
    }
    if (message.status !== 0) {
      writer.uint32(64).int32(message.status);
    }
    if (message.BDs !== undefined) {
      BlockDescriptors.encode(message.BDs, writer.uint32(74).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(82).fork()).join();
    }
    if (message.nextProposer !== "") {
      writer.uint32(90).string(message.nextProposer);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StateInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseStateInfo();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.stateInfoIndex = StateInfoIndex.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.sequencer = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.startHeight = longToNumber(reader.uint64());
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.numBlocks = longToNumber(reader.uint64());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.DAPath = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.creationHeight = longToNumber(reader.uint64());
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.status = reader.int32() as any;
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.BDs = BlockDescriptors.decode(reader, reader.uint32());
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.nextProposer = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): StateInfo {
    return {
      stateInfoIndex: isSet(object.stateInfoIndex) ? StateInfoIndex.fromJSON(object.stateInfoIndex) : undefined,
      sequencer: isSet(object.sequencer) ? globalThis.String(object.sequencer) : "",
      startHeight: isSet(object.startHeight) ? globalThis.Number(object.startHeight) : 0,
      numBlocks: isSet(object.numBlocks) ? globalThis.Number(object.numBlocks) : 0,
      DAPath: isSet(object.DAPath) ? globalThis.String(object.DAPath) : "",
      creationHeight: isSet(object.creationHeight) ? globalThis.Number(object.creationHeight) : 0,
      status: isSet(object.status) ? statusFromJSON(object.status) : 0,
      BDs: isSet(object.BDs) ? BlockDescriptors.fromJSON(object.BDs) : undefined,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      nextProposer: isSet(object.nextProposer) ? globalThis.String(object.nextProposer) : "",
    };
  },

  toJSON(message: StateInfo): unknown {
    const obj: any = {};
    if (message.stateInfoIndex !== undefined) {
      obj.stateInfoIndex = StateInfoIndex.toJSON(message.stateInfoIndex);
    }
    if (message.sequencer !== "") {
      obj.sequencer = message.sequencer;
    }
    if (message.startHeight !== 0) {
      obj.startHeight = Math.round(message.startHeight);
    }
    if (message.numBlocks !== 0) {
      obj.numBlocks = Math.round(message.numBlocks);
    }
    if (message.DAPath !== "") {
      obj.DAPath = message.DAPath;
    }
    if (message.creationHeight !== 0) {
      obj.creationHeight = Math.round(message.creationHeight);
    }
    if (message.status !== 0) {
      obj.status = statusToJSON(message.status);
    }
    if (message.BDs !== undefined) {
      obj.BDs = BlockDescriptors.toJSON(message.BDs);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.nextProposer !== "") {
      obj.nextProposer = message.nextProposer;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StateInfo>, I>>(base?: I): StateInfo {
    return StateInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StateInfo>, I>>(object: I): StateInfo {
    const message = createBaseStateInfo();
    message.stateInfoIndex = (object.stateInfoIndex !== undefined && object.stateInfoIndex !== null)
      ? StateInfoIndex.fromPartial(object.stateInfoIndex)
      : undefined;
    message.sequencer = object.sequencer ?? "";
    message.startHeight = object.startHeight ?? 0;
    message.numBlocks = object.numBlocks ?? 0;
    message.DAPath = object.DAPath ?? "";
    message.creationHeight = object.creationHeight ?? 0;
    message.status = object.status ?? 0;
    message.BDs = (object.BDs !== undefined && object.BDs !== null)
      ? BlockDescriptors.fromPartial(object.BDs)
      : undefined;
    message.createdAt = object.createdAt ?? undefined;
    message.nextProposer = object.nextProposer ?? "";
    return message;
  },
};

function createBaseStateInfoSummary(): StateInfoSummary {
  return { stateInfoIndex: undefined, status: 0, creationHeight: 0 };
}

export const StateInfoSummary: MessageFns<StateInfoSummary> = {
  encode(message: StateInfoSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stateInfoIndex !== undefined) {
      StateInfoIndex.encode(message.stateInfoIndex, writer.uint32(10).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.creationHeight !== 0) {
      writer.uint32(24).uint64(message.creationHeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StateInfoSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseStateInfoSummary();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.stateInfoIndex = StateInfoIndex.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.status = reader.int32() as any;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.creationHeight = longToNumber(reader.uint64());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): StateInfoSummary {
    return {
      stateInfoIndex: isSet(object.stateInfoIndex) ? StateInfoIndex.fromJSON(object.stateInfoIndex) : undefined,
      status: isSet(object.status) ? statusFromJSON(object.status) : 0,
      creationHeight: isSet(object.creationHeight) ? globalThis.Number(object.creationHeight) : 0,
    };
  },

  toJSON(message: StateInfoSummary): unknown {
    const obj: any = {};
    if (message.stateInfoIndex !== undefined) {
      obj.stateInfoIndex = StateInfoIndex.toJSON(message.stateInfoIndex);
    }
    if (message.status !== 0) {
      obj.status = statusToJSON(message.status);
    }
    if (message.creationHeight !== 0) {
      obj.creationHeight = Math.round(message.creationHeight);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StateInfoSummary>, I>>(base?: I): StateInfoSummary {
    return StateInfoSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StateInfoSummary>, I>>(object: I): StateInfoSummary {
    const message = createBaseStateInfoSummary();
    message.stateInfoIndex = (object.stateInfoIndex !== undefined && object.stateInfoIndex !== null)
      ? StateInfoIndex.fromPartial(object.stateInfoIndex)
      : undefined;
    message.status = object.status ?? 0;
    message.creationHeight = object.creationHeight ?? 0;
    return message;
  },
};

function createBaseBlockHeightToFinalizationQueue(): BlockHeightToFinalizationQueue {
  return { creationHeight: 0, finalizationQueue: [], rollappId: "" };
}

export const BlockHeightToFinalizationQueue: MessageFns<BlockHeightToFinalizationQueue> = {
  encode(message: BlockHeightToFinalizationQueue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creationHeight !== 0) {
      writer.uint32(8).uint64(message.creationHeight);
    }
    for (const v of message.finalizationQueue) {
      StateInfoIndex.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.rollappId !== "") {
      writer.uint32(26).string(message.rollappId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BlockHeightToFinalizationQueue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBlockHeightToFinalizationQueue();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.creationHeight = longToNumber(reader.uint64());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.finalizationQueue.push(StateInfoIndex.decode(reader, reader.uint32()));
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.rollappId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): BlockHeightToFinalizationQueue {
    return {
      creationHeight: isSet(object.creationHeight) ? globalThis.Number(object.creationHeight) : 0,
      finalizationQueue: globalThis.Array.isArray(object?.finalizationQueue)
        ? object.finalizationQueue.map((e: any) => StateInfoIndex.fromJSON(e))
        : [],
      rollappId: isSet(object.rollappId)
        ? globalThis.String(object.rollappId)
        : isSet(object.rollapp_id)
        ? globalThis.String(object.rollapp_id)
        : "",
    };
  },

  toJSON(message: BlockHeightToFinalizationQueue): unknown {
    const obj: any = {};
    if (message.creationHeight !== 0) {
      obj.creationHeight = Math.round(message.creationHeight);
    }
    if (message.finalizationQueue?.length) {
      obj.finalizationQueue = message.finalizationQueue.map((e) => StateInfoIndex.toJSON(e));
    }
    if (message.rollappId !== "") {
      obj.rollappId = message.rollappId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BlockHeightToFinalizationQueue>, I>>(base?: I): BlockHeightToFinalizationQueue {
    return BlockHeightToFinalizationQueue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BlockHeightToFinalizationQueue>, I>>(
    object: I,
  ): BlockHeightToFinalizationQueue {
    const message = createBaseBlockHeightToFinalizationQueue();
    message.creationHeight = object.creationHeight ?? 0;
    message.finalizationQueue = object.finalizationQueue?.map((e) => StateInfoIndex.fromPartial(e)) || [];
    message.rollappId = object.rollappId ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
