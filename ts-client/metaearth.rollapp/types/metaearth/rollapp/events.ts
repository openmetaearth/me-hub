// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: metaearth/rollapp/events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { App } from "./app";

export const protobufPackage = "metaearth.rollapp";

export interface EventAppAdded {
  app: App | undefined;
}

export interface EventAppUpdated {
  app: App | undefined;
}

export interface EventAppRemoved {
  app: App | undefined;
}

export interface EventMarkObsoleteRollapps {
  /** ObsoleteRollappNum is a number of rollapps that were marked as obsolete. */
  obsoleteRollappNum: number;
  /** DrsVersions is a list of DRS versions that were marked as obsolete. */
  drsVersions: number[];
}

function createBaseEventAppAdded(): EventAppAdded {
  return { app: undefined };
}

export const EventAppAdded: MessageFns<EventAppAdded> = {
  encode(message: EventAppAdded, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.app !== undefined) {
      App.encode(message.app, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAppAdded {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventAppAdded();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.app = App.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventAppAdded {
    return { app: isSet(object.app) ? App.fromJSON(object.app) : undefined };
  },

  toJSON(message: EventAppAdded): unknown {
    const obj: any = {};
    if (message.app !== undefined) {
      obj.app = App.toJSON(message.app);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventAppAdded>, I>>(base?: I): EventAppAdded {
    return EventAppAdded.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventAppAdded>, I>>(object: I): EventAppAdded {
    const message = createBaseEventAppAdded();
    message.app = (object.app !== undefined && object.app !== null) ? App.fromPartial(object.app) : undefined;
    return message;
  },
};

function createBaseEventAppUpdated(): EventAppUpdated {
  return { app: undefined };
}

export const EventAppUpdated: MessageFns<EventAppUpdated> = {
  encode(message: EventAppUpdated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.app !== undefined) {
      App.encode(message.app, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAppUpdated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventAppUpdated();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.app = App.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventAppUpdated {
    return { app: isSet(object.app) ? App.fromJSON(object.app) : undefined };
  },

  toJSON(message: EventAppUpdated): unknown {
    const obj: any = {};
    if (message.app !== undefined) {
      obj.app = App.toJSON(message.app);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventAppUpdated>, I>>(base?: I): EventAppUpdated {
    return EventAppUpdated.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventAppUpdated>, I>>(object: I): EventAppUpdated {
    const message = createBaseEventAppUpdated();
    message.app = (object.app !== undefined && object.app !== null) ? App.fromPartial(object.app) : undefined;
    return message;
  },
};

function createBaseEventAppRemoved(): EventAppRemoved {
  return { app: undefined };
}

export const EventAppRemoved: MessageFns<EventAppRemoved> = {
  encode(message: EventAppRemoved, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.app !== undefined) {
      App.encode(message.app, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAppRemoved {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventAppRemoved();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.app = App.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventAppRemoved {
    return { app: isSet(object.app) ? App.fromJSON(object.app) : undefined };
  },

  toJSON(message: EventAppRemoved): unknown {
    const obj: any = {};
    if (message.app !== undefined) {
      obj.app = App.toJSON(message.app);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventAppRemoved>, I>>(base?: I): EventAppRemoved {
    return EventAppRemoved.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventAppRemoved>, I>>(object: I): EventAppRemoved {
    const message = createBaseEventAppRemoved();
    message.app = (object.app !== undefined && object.app !== null) ? App.fromPartial(object.app) : undefined;
    return message;
  },
};

function createBaseEventMarkObsoleteRollapps(): EventMarkObsoleteRollapps {
  return { obsoleteRollappNum: 0, drsVersions: [] };
}

export const EventMarkObsoleteRollapps: MessageFns<EventMarkObsoleteRollapps> = {
  encode(message: EventMarkObsoleteRollapps, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.obsoleteRollappNum !== 0) {
      writer.uint32(8).uint64(message.obsoleteRollappNum);
    }
    writer.uint32(18).fork();
    for (const v of message.drsVersions) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventMarkObsoleteRollapps {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventMarkObsoleteRollapps();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.obsoleteRollappNum = longToNumber(reader.uint64());
            continue;
          }
          case 2: {
            if (tag === 16) {
              message.drsVersions.push(reader.uint32());

              continue;
            }

            if (tag === 18) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.drsVersions.push(reader.uint32());
              }

              continue;
            }

            break;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventMarkObsoleteRollapps {
    return {
      obsoleteRollappNum: isSet(object.obsoleteRollappNum)
        ? globalThis.Number(object.obsoleteRollappNum)
        : isSet(object.obsolete_rollapp_num)
        ? globalThis.Number(object.obsolete_rollapp_num)
        : 0,
      drsVersions: globalThis.Array.isArray(object?.drsVersions)
        ? object.drsVersions.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.drs_versions)
        ? object.drs_versions.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: EventMarkObsoleteRollapps): unknown {
    const obj: any = {};
    if (message.obsoleteRollappNum !== 0) {
      obj.obsoleteRollappNum = Math.round(message.obsoleteRollappNum);
    }
    if (message.drsVersions?.length) {
      obj.drsVersions = message.drsVersions.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventMarkObsoleteRollapps>, I>>(base?: I): EventMarkObsoleteRollapps {
    return EventMarkObsoleteRollapps.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventMarkObsoleteRollapps>, I>>(object: I): EventMarkObsoleteRollapps {
    const message = createBaseEventMarkObsoleteRollapps();
    message.obsoleteRollappNum = object.obsoleteRollappNum ?? 0;
    message.drsVersions = object.drsVersions?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
