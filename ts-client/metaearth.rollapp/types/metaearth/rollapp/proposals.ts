// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: metaearth/rollapp/proposals.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GenesisInfo } from "./genesis_info";

export const protobufPackage = "metaearth.rollapp";

export interface MsgRollappFraudProposal {
  /** Authority is the authority address. */
  authority: string;
  /** RollappID is the rollapp id. */
  rollappId: string;
  /** The height of the fraudulent block */
  fraudHeight: number;
  /** Revision of fraudulent block */
  fraudRevision: number;
  /** sequencer address to punish. optional */
  punishSequencerAddress: string;
  /** rewardAddr is bech32 for sdk acc addr */
  rewardee: string;
}

export interface MsgRollappFraudProposalResponse {
}

/** MsgForceGenesisInfoChange defines the Msg/ForceGenesisInfoChange request type */
export interface MsgForceGenesisInfoChange {
  /**
   * Authority is the address that controls the module (defaults to x/gov unless
   * overwritten).
   */
  authority: string;
  /**
   * RollappID is the unique identifier of the rollapp whose genesis info will
   * be changed
   */
  rollappId: string;
  /**
   * NewGenesisInfo contains the new genesis information that will replace the
   * existing one
   */
  newGenesisInfo: GenesisInfo | undefined;
}

/**
 * MsgForceGenesisInfoChangeResponse defines the Msg/ForceGenesisInfoChange
 * response type
 */
export interface MsgForceGenesisInfoChangeResponse {
}

function createBaseMsgRollappFraudProposal(): MsgRollappFraudProposal {
  return { authority: "", rollappId: "", fraudHeight: 0, fraudRevision: 0, punishSequencerAddress: "", rewardee: "" };
}

export const MsgRollappFraudProposal: MessageFns<MsgRollappFraudProposal> = {
  encode(message: MsgRollappFraudProposal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.rollappId !== "") {
      writer.uint32(18).string(message.rollappId);
    }
    if (message.fraudHeight !== 0) {
      writer.uint32(32).uint64(message.fraudHeight);
    }
    if (message.fraudRevision !== 0) {
      writer.uint32(24).uint64(message.fraudRevision);
    }
    if (message.punishSequencerAddress !== "") {
      writer.uint32(50).string(message.punishSequencerAddress);
    }
    if (message.rewardee !== "") {
      writer.uint32(58).string(message.rewardee);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRollappFraudProposal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgRollappFraudProposal();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.authority = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.rollappId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.fraudHeight = longToNumber(reader.uint64());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.fraudRevision = longToNumber(reader.uint64());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.punishSequencerAddress = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.rewardee = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MsgRollappFraudProposal {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      rollappId: isSet(object.rollappId)
        ? globalThis.String(object.rollappId)
        : isSet(object.rollapp_id)
        ? globalThis.String(object.rollapp_id)
        : "",
      fraudHeight: isSet(object.fraudHeight)
        ? globalThis.Number(object.fraudHeight)
        : isSet(object.fraud_height)
        ? globalThis.Number(object.fraud_height)
        : 0,
      fraudRevision: isSet(object.fraudRevision)
        ? globalThis.Number(object.fraudRevision)
        : isSet(object.fraud_revision)
        ? globalThis.Number(object.fraud_revision)
        : 0,
      punishSequencerAddress: isSet(object.punishSequencerAddress)
        ? globalThis.String(object.punishSequencerAddress)
        : isSet(object.punish_sequencer_address)
        ? globalThis.String(object.punish_sequencer_address)
        : "",
      rewardee: isSet(object.rewardee) ? globalThis.String(object.rewardee) : "",
    };
  },

  toJSON(message: MsgRollappFraudProposal): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.rollappId !== "") {
      obj.rollappId = message.rollappId;
    }
    if (message.fraudHeight !== 0) {
      obj.fraudHeight = Math.round(message.fraudHeight);
    }
    if (message.fraudRevision !== 0) {
      obj.fraudRevision = Math.round(message.fraudRevision);
    }
    if (message.punishSequencerAddress !== "") {
      obj.punishSequencerAddress = message.punishSequencerAddress;
    }
    if (message.rewardee !== "") {
      obj.rewardee = message.rewardee;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRollappFraudProposal>, I>>(base?: I): MsgRollappFraudProposal {
    return MsgRollappFraudProposal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRollappFraudProposal>, I>>(object: I): MsgRollappFraudProposal {
    const message = createBaseMsgRollappFraudProposal();
    message.authority = object.authority ?? "";
    message.rollappId = object.rollappId ?? "";
    message.fraudHeight = object.fraudHeight ?? 0;
    message.fraudRevision = object.fraudRevision ?? 0;
    message.punishSequencerAddress = object.punishSequencerAddress ?? "";
    message.rewardee = object.rewardee ?? "";
    return message;
  },
};

function createBaseMsgRollappFraudProposalResponse(): MsgRollappFraudProposalResponse {
  return {};
}

export const MsgRollappFraudProposalResponse: MessageFns<MsgRollappFraudProposalResponse> = {
  encode(_: MsgRollappFraudProposalResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRollappFraudProposalResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgRollappFraudProposalResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): MsgRollappFraudProposalResponse {
    return {};
  },

  toJSON(_: MsgRollappFraudProposalResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRollappFraudProposalResponse>, I>>(base?: I): MsgRollappFraudProposalResponse {
    return MsgRollappFraudProposalResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRollappFraudProposalResponse>, I>>(_: I): MsgRollappFraudProposalResponse {
    const message = createBaseMsgRollappFraudProposalResponse();
    return message;
  },
};

function createBaseMsgForceGenesisInfoChange(): MsgForceGenesisInfoChange {
  return { authority: "", rollappId: "", newGenesisInfo: undefined };
}

export const MsgForceGenesisInfoChange: MessageFns<MsgForceGenesisInfoChange> = {
  encode(message: MsgForceGenesisInfoChange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.rollappId !== "") {
      writer.uint32(18).string(message.rollappId);
    }
    if (message.newGenesisInfo !== undefined) {
      GenesisInfo.encode(message.newGenesisInfo, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgForceGenesisInfoChange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgForceGenesisInfoChange();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.authority = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.rollappId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.newGenesisInfo = GenesisInfo.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MsgForceGenesisInfoChange {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      rollappId: isSet(object.rollappId)
        ? globalThis.String(object.rollappId)
        : isSet(object.rollapp_id)
        ? globalThis.String(object.rollapp_id)
        : "",
      newGenesisInfo: isSet(object.newGenesisInfo)
        ? GenesisInfo.fromJSON(object.newGenesisInfo)
        : isSet(object.new_genesis_info)
        ? GenesisInfo.fromJSON(object.new_genesis_info)
        : undefined,
    };
  },

  toJSON(message: MsgForceGenesisInfoChange): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.rollappId !== "") {
      obj.rollappId = message.rollappId;
    }
    if (message.newGenesisInfo !== undefined) {
      obj.newGenesisInfo = GenesisInfo.toJSON(message.newGenesisInfo);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgForceGenesisInfoChange>, I>>(base?: I): MsgForceGenesisInfoChange {
    return MsgForceGenesisInfoChange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgForceGenesisInfoChange>, I>>(object: I): MsgForceGenesisInfoChange {
    const message = createBaseMsgForceGenesisInfoChange();
    message.authority = object.authority ?? "";
    message.rollappId = object.rollappId ?? "";
    message.newGenesisInfo = (object.newGenesisInfo !== undefined && object.newGenesisInfo !== null)
      ? GenesisInfo.fromPartial(object.newGenesisInfo)
      : undefined;
    return message;
  },
};

function createBaseMsgForceGenesisInfoChangeResponse(): MsgForceGenesisInfoChangeResponse {
  return {};
}

export const MsgForceGenesisInfoChangeResponse: MessageFns<MsgForceGenesisInfoChangeResponse> = {
  encode(_: MsgForceGenesisInfoChangeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgForceGenesisInfoChangeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgForceGenesisInfoChangeResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): MsgForceGenesisInfoChangeResponse {
    return {};
  },

  toJSON(_: MsgForceGenesisInfoChangeResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgForceGenesisInfoChangeResponse>, I>>(
    base?: I,
  ): MsgForceGenesisInfoChangeResponse {
    return MsgForceGenesisInfoChangeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgForceGenesisInfoChangeResponse>, I>>(
    _: I,
  ): MsgForceGenesisInfoChangeResponse {
    const message = createBaseMsgForceGenesisInfoChangeResponse();
    return message;
  },
};

export interface ProposalMsg {
  SubmitRollappFraud(request: MsgRollappFraudProposal): Promise<MsgRollappFraudProposalResponse>;
  ForceGenesisInfoChange(request: MsgForceGenesisInfoChange): Promise<MsgForceGenesisInfoChangeResponse>;
}

export const ProposalMsgServiceName = "metaearth.rollapp.ProposalMsg";
export class ProposalMsgClientImpl implements ProposalMsg {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ProposalMsgServiceName;
    this.rpc = rpc;
    this.SubmitRollappFraud = this.SubmitRollappFraud.bind(this);
    this.ForceGenesisInfoChange = this.ForceGenesisInfoChange.bind(this);
  }
  SubmitRollappFraud(request: MsgRollappFraudProposal): Promise<MsgRollappFraudProposalResponse> {
    const data = MsgRollappFraudProposal.encode(request).finish();
    const promise = this.rpc.request(this.service, "SubmitRollappFraud", data);
    return promise.then((data) => MsgRollappFraudProposalResponse.decode(new BinaryReader(data)));
  }

  ForceGenesisInfoChange(request: MsgForceGenesisInfoChange): Promise<MsgForceGenesisInfoChangeResponse> {
    const data = MsgForceGenesisInfoChange.encode(request).finish();
    const promise = this.rpc.request(this.service, "ForceGenesisInfoChange", data);
    return promise.then((data) => MsgForceGenesisInfoChangeResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
