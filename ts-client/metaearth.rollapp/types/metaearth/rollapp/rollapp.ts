// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: metaearth/rollapp/rollapp.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Coin } from "../../cosmos/base/v1beta1/coin";
import { Timestamp } from "../../google/protobuf/timestamp";
import { GenesisInfo } from "./genesis_info";
import { RollappMetadata } from "./metadata";
import { StateInfoIndex } from "./state_info";

export const protobufPackage = "metaearth.rollapp";

/**
 * RollappGenesisState is a partial repr of the state the hub can expect the
 * rollapp to be in upon genesis
 */
export interface RollappGenesisState {
  /**
   * 0 means unpopulated
   * If populated, it's the proof height that the hub received the genesis
   * transfer packet from the rollapp. If populated, the bridge is considered
   * open. It's not allowed to fork to a height prior to this height.
   */
  transferProofHeight: number;
}

/**
 * Rollapp defines a rollapp object. First, the RollApp is created and then
 * sequencers can be created and attached. The RollApp is identified by
 * rollappId
 */
export interface Rollapp {
  /**
   * The unique identifier of the rollapp chain.
   * The rollapp_id follows the same standard as cosmos chain_id.
   */
  rollappId: string;
  /** owner is the bech32-encoded address of the rollapp owner. */
  owner: string;
  /**
   * genesis_state is a partial repr of the state the hub can expect the rollapp
   * to be in upon genesis
   */
  genesisState:
    | RollappGenesisState
    | undefined;
  /** channel_id will be set to the canonical IBC channel of the rollapp. */
  channelId: string;
  /** metadata is the rollapp metadata */
  metadata:
    | RollappMetadata
    | undefined;
  /** genesis_info keeps immutable rollapp fields */
  genesisInfo:
    | GenesisInfo
    | undefined;
  /**
   * initial_sequencer is an option to preset one or more coma-separated
   * bech32-encoded addresses of the sequencer(s) that are allowed to initially
   * register and serve for this rollapp. if left empty, no sequencer is allowed
   * to register. if set to "*" any sequencer can register.
   */
  initialSequencer: string;
  /**
   * How much a sequencer needs to have at stake to be proposer.
   * Denom is dym. It is empty or ONE coin only. Coins is just a convenience to
   * avoid marshalling issues.
   */
  minSequencerBond: Coin[];
  /** vm_type is the type of rollapp machine: EVM or WASM */
  vmType: Rollapp_VMType;
  /**
   * launched is a boolean that indicates that the Rollapp has been launched and
   * the immutable fields are no longer updatable.
   */
  launched: boolean;
  /**
   * pre_launch_time is the timestamp indicating the time before which the
   * rollapp cannot be started.
   * Set when creating IRO plan for the rollapp
   */
  preLaunchTime:
    | Date
    | undefined;
  /**
   * LivenessEventHeight is the height on the HUB of an upcoming liveness event
   * (slash or jail against the rollapp). 0 means not set
   */
  livenessEventHeight: number;
  /**
   * The height on the HUB that we start counting liveness from. If the rollapp
   * is not active for a long time after this height, a liveness event will
   * happen.
   */
  livenessCountdownStartHeight: number;
  /** Revisions is a list of all the rollapp revisions. */
  revisions: Revision[];
  /**
   * togglable by owner: enable fast finalization via TEE nodes (note: global
   * gov param required too)
   */
  enableTee: boolean;
}

export enum Rollapp_VMType {
  Unspecified = 0,
  EVM = 1,
  WASM = 2,
  UNRECOGNIZED = -1,
}

export function rollapp_VMTypeFromJSON(object: any): Rollapp_VMType {
  switch (object) {
    case 0:
    case "Unspecified":
      return Rollapp_VMType.Unspecified;
    case 1:
    case "EVM":
      return Rollapp_VMType.EVM;
    case 2:
    case "WASM":
      return Rollapp_VMType.WASM;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Rollapp_VMType.UNRECOGNIZED;
  }
}

export function rollapp_VMTypeToJSON(object: Rollapp_VMType): string {
  switch (object) {
    case Rollapp_VMType.Unspecified:
      return "Unspecified";
    case Rollapp_VMType.EVM:
      return "EVM";
    case Rollapp_VMType.WASM:
      return "WASM";
    case Rollapp_VMType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Revision is a representation of the rollapp revision. */
export interface Revision {
  /** Number is the revision number of the rollapp. Always start with 0 revision. */
  number: number;
  /** StartHeight is the first height of the rollapp when the revision started. */
  startHeight: number;
}

/** Rollapp summary is a compact representation of Rollapp */
export interface RollappSummary {
  /**
   * The unique identifier of the rollapp chain.
   * The rollappId follows the same standard as cosmos chain_id.
   */
  rollappId: string;
  /** Defines the index of the last rollapp UpdateState. */
  latestStateIndex:
    | StateInfoIndex
    | undefined;
  /** Defines the index of the last rollapp UpdateState that was finalized. */
  latestFinalizedStateIndex:
    | StateInfoIndex
    | undefined;
  /** TODO: */
  latestHeight: number;
  /** TODO: */
  latestFinalizedHeight: number;
}

function createBaseRollappGenesisState(): RollappGenesisState {
  return { transferProofHeight: 0 };
}

export const RollappGenesisState: MessageFns<RollappGenesisState> = {
  encode(message: RollappGenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.transferProofHeight !== 0) {
      writer.uint32(24).uint64(message.transferProofHeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RollappGenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRollappGenesisState();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.transferProofHeight = longToNumber(reader.uint64());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RollappGenesisState {
    return {
      transferProofHeight: isSet(object.transferProofHeight)
        ? globalThis.Number(object.transferProofHeight)
        : isSet(object.transfer_proof_height)
        ? globalThis.Number(object.transfer_proof_height)
        : 0,
    };
  },

  toJSON(message: RollappGenesisState): unknown {
    const obj: any = {};
    if (message.transferProofHeight !== 0) {
      obj.transferProofHeight = Math.round(message.transferProofHeight);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RollappGenesisState>, I>>(base?: I): RollappGenesisState {
    return RollappGenesisState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RollappGenesisState>, I>>(object: I): RollappGenesisState {
    const message = createBaseRollappGenesisState();
    message.transferProofHeight = object.transferProofHeight ?? 0;
    return message;
  },
};

function createBaseRollapp(): Rollapp {
  return {
    rollappId: "",
    owner: "",
    genesisState: undefined,
    channelId: "",
    metadata: undefined,
    genesisInfo: undefined,
    initialSequencer: "",
    minSequencerBond: [],
    vmType: 0,
    launched: false,
    preLaunchTime: undefined,
    livenessEventHeight: 0,
    livenessCountdownStartHeight: 0,
    revisions: [],
    enableTee: false,
  };
}

export const Rollapp: MessageFns<Rollapp> = {
  encode(message: Rollapp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollappId !== "") {
      writer.uint32(10).string(message.rollappId);
    }
    if (message.owner !== "") {
      writer.uint32(18).string(message.owner);
    }
    if (message.genesisState !== undefined) {
      RollappGenesisState.encode(message.genesisState, writer.uint32(58).fork()).join();
    }
    if (message.channelId !== "") {
      writer.uint32(66).string(message.channelId);
    }
    if (message.metadata !== undefined) {
      RollappMetadata.encode(message.metadata, writer.uint32(90).fork()).join();
    }
    if (message.genesisInfo !== undefined) {
      GenesisInfo.encode(message.genesisInfo, writer.uint32(98).fork()).join();
    }
    if (message.initialSequencer !== "") {
      writer.uint32(106).string(message.initialSequencer);
    }
    for (const v of message.minSequencerBond) {
      Coin.encode(v!, writer.uint32(162).fork()).join();
    }
    if (message.vmType !== 0) {
      writer.uint32(112).int32(message.vmType);
    }
    if (message.launched !== false) {
      writer.uint32(120).bool(message.launched);
    }
    if (message.preLaunchTime !== undefined) {
      Timestamp.encode(toTimestamp(message.preLaunchTime), writer.uint32(130).fork()).join();
    }
    if (message.livenessEventHeight !== 0) {
      writer.uint32(136).int64(message.livenessEventHeight);
    }
    if (message.livenessCountdownStartHeight !== 0) {
      writer.uint32(144).int64(message.livenessCountdownStartHeight);
    }
    for (const v of message.revisions) {
      Revision.encode(v!, writer.uint32(154).fork()).join();
    }
    if (message.enableTee !== false) {
      writer.uint32(168).bool(message.enableTee);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Rollapp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRollapp();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.rollappId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.owner = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.genesisState = RollappGenesisState.decode(reader, reader.uint32());
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.channelId = reader.string();
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.metadata = RollappMetadata.decode(reader, reader.uint32());
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.genesisInfo = GenesisInfo.decode(reader, reader.uint32());
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.initialSequencer = reader.string();
            continue;
          }
          case 20: {
            if (tag !== 162) {
              break;
            }

            message.minSequencerBond.push(Coin.decode(reader, reader.uint32()));
            continue;
          }
          case 14: {
            if (tag !== 112) {
              break;
            }

            message.vmType = reader.int32() as any;
            continue;
          }
          case 15: {
            if (tag !== 120) {
              break;
            }

            message.launched = reader.bool();
            continue;
          }
          case 16: {
            if (tag !== 130) {
              break;
            }

            message.preLaunchTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 17: {
            if (tag !== 136) {
              break;
            }

            message.livenessEventHeight = longToNumber(reader.int64());
            continue;
          }
          case 18: {
            if (tag !== 144) {
              break;
            }

            message.livenessCountdownStartHeight = longToNumber(reader.int64());
            continue;
          }
          case 19: {
            if (tag !== 154) {
              break;
            }

            message.revisions.push(Revision.decode(reader, reader.uint32()));
            continue;
          }
          case 21: {
            if (tag !== 168) {
              break;
            }

            message.enableTee = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Rollapp {
    return {
      rollappId: isSet(object.rollappId)
        ? globalThis.String(object.rollappId)
        : isSet(object.rollapp_id)
        ? globalThis.String(object.rollapp_id)
        : "",
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      genesisState: isSet(object.genesisState)
        ? RollappGenesisState.fromJSON(object.genesisState)
        : isSet(object.genesis_state)
        ? RollappGenesisState.fromJSON(object.genesis_state)
        : undefined,
      channelId: isSet(object.channelId)
        ? globalThis.String(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.String(object.channel_id)
        : "",
      metadata: isSet(object.metadata) ? RollappMetadata.fromJSON(object.metadata) : undefined,
      genesisInfo: isSet(object.genesisInfo)
        ? GenesisInfo.fromJSON(object.genesisInfo)
        : isSet(object.genesis_info)
        ? GenesisInfo.fromJSON(object.genesis_info)
        : undefined,
      initialSequencer: isSet(object.initialSequencer)
        ? globalThis.String(object.initialSequencer)
        : isSet(object.initial_sequencer)
        ? globalThis.String(object.initial_sequencer)
        : "",
      minSequencerBond: globalThis.Array.isArray(object?.minSequencerBond)
        ? object.minSequencerBond.map((e: any) => Coin.fromJSON(e))
        : globalThis.Array.isArray(object?.min_sequencer_bond)
        ? object.min_sequencer_bond.map((e: any) => Coin.fromJSON(e))
        : [],
      vmType: isSet(object.vmType)
        ? rollapp_VMTypeFromJSON(object.vmType)
        : isSet(object.vm_type)
        ? rollapp_VMTypeFromJSON(object.vm_type)
        : 0,
      launched: isSet(object.launched) ? globalThis.Boolean(object.launched) : false,
      preLaunchTime: isSet(object.preLaunchTime)
        ? fromJsonTimestamp(object.preLaunchTime)
        : isSet(object.pre_launch_time)
        ? fromJsonTimestamp(object.pre_launch_time)
        : undefined,
      livenessEventHeight: isSet(object.livenessEventHeight)
        ? globalThis.Number(object.livenessEventHeight)
        : isSet(object.liveness_event_height)
        ? globalThis.Number(object.liveness_event_height)
        : 0,
      livenessCountdownStartHeight: isSet(object.livenessCountdownStartHeight)
        ? globalThis.Number(object.livenessCountdownStartHeight)
        : isSet(object.liveness_countdown_start_height)
        ? globalThis.Number(object.liveness_countdown_start_height)
        : 0,
      revisions: globalThis.Array.isArray(object?.revisions)
        ? object.revisions.map((e: any) => Revision.fromJSON(e))
        : [],
      enableTee: isSet(object.enableTee)
        ? globalThis.Boolean(object.enableTee)
        : isSet(object.enable_tee)
        ? globalThis.Boolean(object.enable_tee)
        : false,
    };
  },

  toJSON(message: Rollapp): unknown {
    const obj: any = {};
    if (message.rollappId !== "") {
      obj.rollappId = message.rollappId;
    }
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.genesisState !== undefined) {
      obj.genesisState = RollappGenesisState.toJSON(message.genesisState);
    }
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    if (message.metadata !== undefined) {
      obj.metadata = RollappMetadata.toJSON(message.metadata);
    }
    if (message.genesisInfo !== undefined) {
      obj.genesisInfo = GenesisInfo.toJSON(message.genesisInfo);
    }
    if (message.initialSequencer !== "") {
      obj.initialSequencer = message.initialSequencer;
    }
    if (message.minSequencerBond?.length) {
      obj.minSequencerBond = message.minSequencerBond.map((e) => Coin.toJSON(e));
    }
    if (message.vmType !== 0) {
      obj.vmType = rollapp_VMTypeToJSON(message.vmType);
    }
    if (message.launched !== false) {
      obj.launched = message.launched;
    }
    if (message.preLaunchTime !== undefined) {
      obj.preLaunchTime = message.preLaunchTime.toISOString();
    }
    if (message.livenessEventHeight !== 0) {
      obj.livenessEventHeight = Math.round(message.livenessEventHeight);
    }
    if (message.livenessCountdownStartHeight !== 0) {
      obj.livenessCountdownStartHeight = Math.round(message.livenessCountdownStartHeight);
    }
    if (message.revisions?.length) {
      obj.revisions = message.revisions.map((e) => Revision.toJSON(e));
    }
    if (message.enableTee !== false) {
      obj.enableTee = message.enableTee;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Rollapp>, I>>(base?: I): Rollapp {
    return Rollapp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Rollapp>, I>>(object: I): Rollapp {
    const message = createBaseRollapp();
    message.rollappId = object.rollappId ?? "";
    message.owner = object.owner ?? "";
    message.genesisState = (object.genesisState !== undefined && object.genesisState !== null)
      ? RollappGenesisState.fromPartial(object.genesisState)
      : undefined;
    message.channelId = object.channelId ?? "";
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? RollappMetadata.fromPartial(object.metadata)
      : undefined;
    message.genesisInfo = (object.genesisInfo !== undefined && object.genesisInfo !== null)
      ? GenesisInfo.fromPartial(object.genesisInfo)
      : undefined;
    message.initialSequencer = object.initialSequencer ?? "";
    message.minSequencerBond = object.minSequencerBond?.map((e) => Coin.fromPartial(e)) || [];
    message.vmType = object.vmType ?? 0;
    message.launched = object.launched ?? false;
    message.preLaunchTime = object.preLaunchTime ?? undefined;
    message.livenessEventHeight = object.livenessEventHeight ?? 0;
    message.livenessCountdownStartHeight = object.livenessCountdownStartHeight ?? 0;
    message.revisions = object.revisions?.map((e) => Revision.fromPartial(e)) || [];
    message.enableTee = object.enableTee ?? false;
    return message;
  },
};

function createBaseRevision(): Revision {
  return { number: 0, startHeight: 0 };
}

export const Revision: MessageFns<Revision> = {
  encode(message: Revision, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.number !== 0) {
      writer.uint32(152).uint64(message.number);
    }
    if (message.startHeight !== 0) {
      writer.uint32(160).uint64(message.startHeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Revision {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRevision();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 19: {
            if (tag !== 152) {
              break;
            }

            message.number = longToNumber(reader.uint64());
            continue;
          }
          case 20: {
            if (tag !== 160) {
              break;
            }

            message.startHeight = longToNumber(reader.uint64());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Revision {
    return {
      number: isSet(object.number) ? globalThis.Number(object.number) : 0,
      startHeight: isSet(object.startHeight)
        ? globalThis.Number(object.startHeight)
        : isSet(object.start_height)
        ? globalThis.Number(object.start_height)
        : 0,
    };
  },

  toJSON(message: Revision): unknown {
    const obj: any = {};
    if (message.number !== 0) {
      obj.number = Math.round(message.number);
    }
    if (message.startHeight !== 0) {
      obj.startHeight = Math.round(message.startHeight);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Revision>, I>>(base?: I): Revision {
    return Revision.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Revision>, I>>(object: I): Revision {
    const message = createBaseRevision();
    message.number = object.number ?? 0;
    message.startHeight = object.startHeight ?? 0;
    return message;
  },
};

function createBaseRollappSummary(): RollappSummary {
  return {
    rollappId: "",
    latestStateIndex: undefined,
    latestFinalizedStateIndex: undefined,
    latestHeight: 0,
    latestFinalizedHeight: 0,
  };
}

export const RollappSummary: MessageFns<RollappSummary> = {
  encode(message: RollappSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollappId !== "") {
      writer.uint32(10).string(message.rollappId);
    }
    if (message.latestStateIndex !== undefined) {
      StateInfoIndex.encode(message.latestStateIndex, writer.uint32(18).fork()).join();
    }
    if (message.latestFinalizedStateIndex !== undefined) {
      StateInfoIndex.encode(message.latestFinalizedStateIndex, writer.uint32(26).fork()).join();
    }
    if (message.latestHeight !== 0) {
      writer.uint32(32).uint64(message.latestHeight);
    }
    if (message.latestFinalizedHeight !== 0) {
      writer.uint32(40).uint64(message.latestFinalizedHeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RollappSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRollappSummary();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.rollappId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.latestStateIndex = StateInfoIndex.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.latestFinalizedStateIndex = StateInfoIndex.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.latestHeight = longToNumber(reader.uint64());
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.latestFinalizedHeight = longToNumber(reader.uint64());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RollappSummary {
    return {
      rollappId: isSet(object.rollappId) ? globalThis.String(object.rollappId) : "",
      latestStateIndex: isSet(object.latestStateIndex) ? StateInfoIndex.fromJSON(object.latestStateIndex) : undefined,
      latestFinalizedStateIndex: isSet(object.latestFinalizedStateIndex)
        ? StateInfoIndex.fromJSON(object.latestFinalizedStateIndex)
        : undefined,
      latestHeight: isSet(object.latestHeight) ? globalThis.Number(object.latestHeight) : 0,
      latestFinalizedHeight: isSet(object.latestFinalizedHeight) ? globalThis.Number(object.latestFinalizedHeight) : 0,
    };
  },

  toJSON(message: RollappSummary): unknown {
    const obj: any = {};
    if (message.rollappId !== "") {
      obj.rollappId = message.rollappId;
    }
    if (message.latestStateIndex !== undefined) {
      obj.latestStateIndex = StateInfoIndex.toJSON(message.latestStateIndex);
    }
    if (message.latestFinalizedStateIndex !== undefined) {
      obj.latestFinalizedStateIndex = StateInfoIndex.toJSON(message.latestFinalizedStateIndex);
    }
    if (message.latestHeight !== 0) {
      obj.latestHeight = Math.round(message.latestHeight);
    }
    if (message.latestFinalizedHeight !== 0) {
      obj.latestFinalizedHeight = Math.round(message.latestFinalizedHeight);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RollappSummary>, I>>(base?: I): RollappSummary {
    return RollappSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RollappSummary>, I>>(object: I): RollappSummary {
    const message = createBaseRollappSummary();
    message.rollappId = object.rollappId ?? "";
    message.latestStateIndex = (object.latestStateIndex !== undefined && object.latestStateIndex !== null)
      ? StateInfoIndex.fromPartial(object.latestStateIndex)
      : undefined;
    message.latestFinalizedStateIndex =
      (object.latestFinalizedStateIndex !== undefined && object.latestFinalizedStateIndex !== null)
        ? StateInfoIndex.fromPartial(object.latestFinalizedStateIndex)
        : undefined;
    message.latestHeight = object.latestHeight ?? 0;
    message.latestFinalizedHeight = object.latestFinalizedHeight ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
