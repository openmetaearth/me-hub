// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: metaearth/rollapp/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { App } from "./app";
import { LivenessEvent } from "./liveness";
import { Params } from "./params";
import { Rollapp } from "./rollapp";
import { BlockHeightToFinalizationQueue, StateInfo, StateInfoIndex } from "./state_info";

export const protobufPackage = "metaearth.rollapp";

/** GenesisState defines the rollapp module's genesis state. */
export interface GenesisState {
  params: Params | undefined;
  rollappList: Rollapp[];
  stateInfoList: StateInfo[];
  latestStateInfoIndexList: StateInfoIndex[];
  latestFinalizedStateIndexList: StateInfoIndex[];
  blockHeightToFinalizationQueueList: BlockHeightToFinalizationQueue[];
  /** LivenessEvents are scheduled upcoming liveness events */
  livenessEvents: LivenessEvent[];
  appList: App[];
  registeredDenoms: RollappRegisteredDenoms[];
  sequencerHeightPairs: SequencerHeightPair[];
  /** ObsoleteDrsVersions is a list of DRS versions that are marked obsolete */
  obsoleteDrsVersions: number[];
}

export interface SequencerHeightPair {
  /** sequencer addr to lookup in x/sequencer */
  sequencer: string;
  /** rollapp chain height */
  height: number;
}

export interface RollappRegisteredDenoms {
  rollappId: string;
  denoms: string[];
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    rollappList: [],
    stateInfoList: [],
    latestStateInfoIndexList: [],
    latestFinalizedStateIndexList: [],
    blockHeightToFinalizationQueueList: [],
    livenessEvents: [],
    appList: [],
    registeredDenoms: [],
    sequencerHeightPairs: [],
    obsoleteDrsVersions: [],
  };
}

export const GenesisState: MessageFns<GenesisState> = {
  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    for (const v of message.rollappList) {
      Rollapp.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.stateInfoList) {
      StateInfo.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.latestStateInfoIndexList) {
      StateInfoIndex.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.latestFinalizedStateIndexList) {
      StateInfoIndex.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.blockHeightToFinalizationQueueList) {
      BlockHeightToFinalizationQueue.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.livenessEvents) {
      LivenessEvent.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.appList) {
      App.encode(v!, writer.uint32(66).fork()).join();
    }
    for (const v of message.registeredDenoms) {
      RollappRegisteredDenoms.encode(v!, writer.uint32(74).fork()).join();
    }
    for (const v of message.sequencerHeightPairs) {
      SequencerHeightPair.encode(v!, writer.uint32(82).fork()).join();
    }
    writer.uint32(90).fork();
    for (const v of message.obsoleteDrsVersions) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGenesisState();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.params = Params.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.rollappList.push(Rollapp.decode(reader, reader.uint32()));
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.stateInfoList.push(StateInfo.decode(reader, reader.uint32()));
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.latestStateInfoIndexList.push(StateInfoIndex.decode(reader, reader.uint32()));
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.latestFinalizedStateIndexList.push(StateInfoIndex.decode(reader, reader.uint32()));
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.blockHeightToFinalizationQueueList.push(
              BlockHeightToFinalizationQueue.decode(reader, reader.uint32()),
            );
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.livenessEvents.push(LivenessEvent.decode(reader, reader.uint32()));
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.appList.push(App.decode(reader, reader.uint32()));
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.registeredDenoms.push(RollappRegisteredDenoms.decode(reader, reader.uint32()));
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.sequencerHeightPairs.push(SequencerHeightPair.decode(reader, reader.uint32()));
            continue;
          }
          case 11: {
            if (tag === 88) {
              message.obsoleteDrsVersions.push(reader.uint32());

              continue;
            }

            if (tag === 90) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.obsoleteDrsVersions.push(reader.uint32());
              }

              continue;
            }

            break;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      rollappList: globalThis.Array.isArray(object?.rollappList)
        ? object.rollappList.map((e: any) => Rollapp.fromJSON(e))
        : [],
      stateInfoList: globalThis.Array.isArray(object?.stateInfoList)
        ? object.stateInfoList.map((e: any) => StateInfo.fromJSON(e))
        : [],
      latestStateInfoIndexList: globalThis.Array.isArray(object?.latestStateInfoIndexList)
        ? object.latestStateInfoIndexList.map((e: any) => StateInfoIndex.fromJSON(e))
        : [],
      latestFinalizedStateIndexList: globalThis.Array.isArray(object?.latestFinalizedStateIndexList)
        ? object.latestFinalizedStateIndexList.map((e: any) => StateInfoIndex.fromJSON(e))
        : [],
      blockHeightToFinalizationQueueList: globalThis.Array.isArray(object?.blockHeightToFinalizationQueueList)
        ? object.blockHeightToFinalizationQueueList.map((e: any) => BlockHeightToFinalizationQueue.fromJSON(e))
        : [],
      livenessEvents: globalThis.Array.isArray(object?.livenessEvents)
        ? object.livenessEvents.map((e: any) => LivenessEvent.fromJSON(e))
        : [],
      appList: globalThis.Array.isArray(object?.appList)
        ? object.appList.map((e: any) => App.fromJSON(e))
        : [],
      registeredDenoms: globalThis.Array.isArray(object?.registeredDenoms)
        ? object.registeredDenoms.map((e: any) => RollappRegisteredDenoms.fromJSON(e))
        : [],
      sequencerHeightPairs: globalThis.Array.isArray(object?.sequencerHeightPairs)
        ? object.sequencerHeightPairs.map((e: any) => SequencerHeightPair.fromJSON(e))
        : [],
      obsoleteDrsVersions: globalThis.Array.isArray(object?.obsoleteDrsVersions)
        ? object.obsoleteDrsVersions.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.obsolete_drs_versions)
        ? object.obsolete_drs_versions.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.rollappList?.length) {
      obj.rollappList = message.rollappList.map((e) => Rollapp.toJSON(e));
    }
    if (message.stateInfoList?.length) {
      obj.stateInfoList = message.stateInfoList.map((e) => StateInfo.toJSON(e));
    }
    if (message.latestStateInfoIndexList?.length) {
      obj.latestStateInfoIndexList = message.latestStateInfoIndexList.map((e) => StateInfoIndex.toJSON(e));
    }
    if (message.latestFinalizedStateIndexList?.length) {
      obj.latestFinalizedStateIndexList = message.latestFinalizedStateIndexList.map((e) => StateInfoIndex.toJSON(e));
    }
    if (message.blockHeightToFinalizationQueueList?.length) {
      obj.blockHeightToFinalizationQueueList = message.blockHeightToFinalizationQueueList.map((e) =>
        BlockHeightToFinalizationQueue.toJSON(e)
      );
    }
    if (message.livenessEvents?.length) {
      obj.livenessEvents = message.livenessEvents.map((e) => LivenessEvent.toJSON(e));
    }
    if (message.appList?.length) {
      obj.appList = message.appList.map((e) => App.toJSON(e));
    }
    if (message.registeredDenoms?.length) {
      obj.registeredDenoms = message.registeredDenoms.map((e) => RollappRegisteredDenoms.toJSON(e));
    }
    if (message.sequencerHeightPairs?.length) {
      obj.sequencerHeightPairs = message.sequencerHeightPairs.map((e) => SequencerHeightPair.toJSON(e));
    }
    if (message.obsoleteDrsVersions?.length) {
      obj.obsoleteDrsVersions = message.obsoleteDrsVersions.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenesisState>, I>>(base?: I): GenesisState {
    return GenesisState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenesisState>, I>>(object: I): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.rollappList = object.rollappList?.map((e) => Rollapp.fromPartial(e)) || [];
    message.stateInfoList = object.stateInfoList?.map((e) => StateInfo.fromPartial(e)) || [];
    message.latestStateInfoIndexList = object.latestStateInfoIndexList?.map((e) => StateInfoIndex.fromPartial(e)) || [];
    message.latestFinalizedStateIndexList =
      object.latestFinalizedStateIndexList?.map((e) => StateInfoIndex.fromPartial(e)) || [];
    message.blockHeightToFinalizationQueueList =
      object.blockHeightToFinalizationQueueList?.map((e) => BlockHeightToFinalizationQueue.fromPartial(e)) || [];
    message.livenessEvents = object.livenessEvents?.map((e) => LivenessEvent.fromPartial(e)) || [];
    message.appList = object.appList?.map((e) => App.fromPartial(e)) || [];
    message.registeredDenoms = object.registeredDenoms?.map((e) => RollappRegisteredDenoms.fromPartial(e)) || [];
    message.sequencerHeightPairs = object.sequencerHeightPairs?.map((e) => SequencerHeightPair.fromPartial(e)) || [];
    message.obsoleteDrsVersions = object.obsoleteDrsVersions?.map((e) => e) || [];
    return message;
  },
};

function createBaseSequencerHeightPair(): SequencerHeightPair {
  return { sequencer: "", height: 0 };
}

export const SequencerHeightPair: MessageFns<SequencerHeightPair> = {
  encode(message: SequencerHeightPair, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sequencer !== "") {
      writer.uint32(10).string(message.sequencer);
    }
    if (message.height !== 0) {
      writer.uint32(16).uint64(message.height);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SequencerHeightPair {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSequencerHeightPair();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sequencer = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.height = longToNumber(reader.uint64());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SequencerHeightPair {
    return {
      sequencer: isSet(object.sequencer) ? globalThis.String(object.sequencer) : "",
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
    };
  },

  toJSON(message: SequencerHeightPair): unknown {
    const obj: any = {};
    if (message.sequencer !== "") {
      obj.sequencer = message.sequencer;
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SequencerHeightPair>, I>>(base?: I): SequencerHeightPair {
    return SequencerHeightPair.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SequencerHeightPair>, I>>(object: I): SequencerHeightPair {
    const message = createBaseSequencerHeightPair();
    message.sequencer = object.sequencer ?? "";
    message.height = object.height ?? 0;
    return message;
  },
};

function createBaseRollappRegisteredDenoms(): RollappRegisteredDenoms {
  return { rollappId: "", denoms: [] };
}

export const RollappRegisteredDenoms: MessageFns<RollappRegisteredDenoms> = {
  encode(message: RollappRegisteredDenoms, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollappId !== "") {
      writer.uint32(10).string(message.rollappId);
    }
    for (const v of message.denoms) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RollappRegisteredDenoms {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRollappRegisteredDenoms();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.rollappId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.denoms.push(reader.string());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RollappRegisteredDenoms {
    return {
      rollappId: isSet(object.rollappId)
        ? globalThis.String(object.rollappId)
        : isSet(object.rollapp_id)
        ? globalThis.String(object.rollapp_id)
        : "",
      denoms: globalThis.Array.isArray(object?.denoms) ? object.denoms.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: RollappRegisteredDenoms): unknown {
    const obj: any = {};
    if (message.rollappId !== "") {
      obj.rollappId = message.rollappId;
    }
    if (message.denoms?.length) {
      obj.denoms = message.denoms;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RollappRegisteredDenoms>, I>>(base?: I): RollappRegisteredDenoms {
    return RollappRegisteredDenoms.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RollappRegisteredDenoms>, I>>(object: I): RollappRegisteredDenoms {
    const message = createBaseRollappRegisteredDenoms();
    message.rollappId = object.rollappId ?? "";
    message.denoms = object.denoms?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
