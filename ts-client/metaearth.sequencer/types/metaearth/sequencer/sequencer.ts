// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: metaearth/sequencer/sequencer.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Coin } from "../../cosmos/base/v1beta1/coin";
import { Any } from "../../google/protobuf/any";
import { Timestamp } from "../../google/protobuf/timestamp";
import { SequencerMetadata } from "./metadata";
import { OperatingStatus, operatingStatusFromJSON, operatingStatusToJSON } from "./operating_status";

export const protobufPackage = "metaearth.sequencer";

/**
 * Sequencer defines a sequencer identified by its' address (sequencerAddress).
 * The sequencer could be attached to only one rollapp (rollappId).
 */
export interface Sequencer {
  /**
   * Address is the bech32-encoded address of the sequencer account which is the
   * account that the message was sent from.
   */
  address: string;
  /**
   * DymintPubKey is the public key of the sequencers' dymint client, as a
   * Protobuf Any.
   */
  dymintPubKey:
    | Any
    | undefined;
  /** RollappId defines the rollapp to which the sequencer belongs. */
  rollappId: string;
  /** SequencerMetadata defines the extra information for the sequencer. */
  metadata:
    | SequencerMetadata
    | undefined;
  /** OperatingStatus is the sequencer status (bonded/unbonded). */
  status: OperatingStatus;
  /**
   * OptedIn : when true and bonded, the sequencer can be chosen as proposer or
   * successor has no effect if already proposer or successor
   */
  optedIn: boolean;
  /**
   * Tokens: A coins which should always be one dym coin. It's the amount of
   * tokens the sequencer has given to the module.
   */
  tokens: Coin[];
  /**
   * NoticePeriodTime defines the time when the sequencer will finish it's
   * notice period. Zero means not started.
   */
  noticePeriodTime:
    | Date
    | undefined;
  /** RewardAddr is a bech32 encoded sdk acc address */
  rewardAddr: string;
  /**
   * WhitelistedRelayers is an array of the whitelisted relayer addresses.
   * Addresses are bech32-encoded strings.
   */
  whitelistedRelayers: string[];
  /**
   * how badly behaved sequencer is, can incur penalties (kicking) when high
   * 0 is good/default, more is worse
   */
  dishonor: number;
}

function createBaseSequencer(): Sequencer {
  return {
    address: "",
    dymintPubKey: undefined,
    rollappId: "",
    metadata: undefined,
    status: 0,
    optedIn: false,
    tokens: [],
    noticePeriodTime: undefined,
    rewardAddr: "",
    whitelistedRelayers: [],
    dishonor: 0,
  };
}

export const Sequencer: MessageFns<Sequencer> = {
  encode(message: Sequencer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.dymintPubKey !== undefined) {
      Any.encode(message.dymintPubKey, writer.uint32(18).fork()).join();
    }
    if (message.rollappId !== "") {
      writer.uint32(26).string(message.rollappId);
    }
    if (message.metadata !== undefined) {
      SequencerMetadata.encode(message.metadata, writer.uint32(34).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(56).int32(message.status);
    }
    if (message.optedIn !== false) {
      writer.uint32(112).bool(message.optedIn);
    }
    for (const v of message.tokens) {
      Coin.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.noticePeriodTime !== undefined) {
      Timestamp.encode(toTimestamp(message.noticePeriodTime), writer.uint32(90).fork()).join();
    }
    if (message.rewardAddr !== "") {
      writer.uint32(98).string(message.rewardAddr);
    }
    for (const v of message.whitelistedRelayers) {
      writer.uint32(106).string(v!);
    }
    if (message.dishonor !== 0) {
      writer.uint32(120).uint64(message.dishonor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Sequencer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSequencer();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.address = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.dymintPubKey = Any.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.rollappId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.metadata = SequencerMetadata.decode(reader, reader.uint32());
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.status = reader.int32() as any;
            continue;
          }
          case 14: {
            if (tag !== 112) {
              break;
            }

            message.optedIn = reader.bool();
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.tokens.push(Coin.decode(reader, reader.uint32()));
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.noticePeriodTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.rewardAddr = reader.string();
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.whitelistedRelayers.push(reader.string());
            continue;
          }
          case 15: {
            if (tag !== 120) {
              break;
            }

            message.dishonor = longToNumber(reader.uint64());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Sequencer {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      dymintPubKey: isSet(object.dymintPubKey) ? Any.fromJSON(object.dymintPubKey) : undefined,
      rollappId: isSet(object.rollappId) ? globalThis.String(object.rollappId) : "",
      metadata: isSet(object.metadata) ? SequencerMetadata.fromJSON(object.metadata) : undefined,
      status: isSet(object.status) ? operatingStatusFromJSON(object.status) : 0,
      optedIn: isSet(object.optedIn)
        ? globalThis.Boolean(object.optedIn)
        : isSet(object.opted_in)
        ? globalThis.Boolean(object.opted_in)
        : false,
      tokens: globalThis.Array.isArray(object?.tokens) ? object.tokens.map((e: any) => Coin.fromJSON(e)) : [],
      noticePeriodTime: isSet(object.noticePeriodTime)
        ? fromJsonTimestamp(object.noticePeriodTime)
        : isSet(object.notice_period_time)
        ? fromJsonTimestamp(object.notice_period_time)
        : undefined,
      rewardAddr: isSet(object.rewardAddr)
        ? globalThis.String(object.rewardAddr)
        : isSet(object.reward_addr)
        ? globalThis.String(object.reward_addr)
        : "",
      whitelistedRelayers: globalThis.Array.isArray(object?.whitelistedRelayers)
        ? object.whitelistedRelayers.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.whitelisted_relayers)
        ? object.whitelisted_relayers.map((e: any) => globalThis.String(e))
        : [],
      dishonor: isSet(object.dishonor) ? globalThis.Number(object.dishonor) : 0,
    };
  },

  toJSON(message: Sequencer): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.dymintPubKey !== undefined) {
      obj.dymintPubKey = Any.toJSON(message.dymintPubKey);
    }
    if (message.rollappId !== "") {
      obj.rollappId = message.rollappId;
    }
    if (message.metadata !== undefined) {
      obj.metadata = SequencerMetadata.toJSON(message.metadata);
    }
    if (message.status !== 0) {
      obj.status = operatingStatusToJSON(message.status);
    }
    if (message.optedIn !== false) {
      obj.optedIn = message.optedIn;
    }
    if (message.tokens?.length) {
      obj.tokens = message.tokens.map((e) => Coin.toJSON(e));
    }
    if (message.noticePeriodTime !== undefined) {
      obj.noticePeriodTime = message.noticePeriodTime.toISOString();
    }
    if (message.rewardAddr !== "") {
      obj.rewardAddr = message.rewardAddr;
    }
    if (message.whitelistedRelayers?.length) {
      obj.whitelistedRelayers = message.whitelistedRelayers;
    }
    if (message.dishonor !== 0) {
      obj.dishonor = Math.round(message.dishonor);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Sequencer>, I>>(base?: I): Sequencer {
    return Sequencer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Sequencer>, I>>(object: I): Sequencer {
    const message = createBaseSequencer();
    message.address = object.address ?? "";
    message.dymintPubKey = (object.dymintPubKey !== undefined && object.dymintPubKey !== null)
      ? Any.fromPartial(object.dymintPubKey)
      : undefined;
    message.rollappId = object.rollappId ?? "";
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? SequencerMetadata.fromPartial(object.metadata)
      : undefined;
    message.status = object.status ?? 0;
    message.optedIn = object.optedIn ?? false;
    message.tokens = object.tokens?.map((e) => Coin.fromPartial(e)) || [];
    message.noticePeriodTime = object.noticePeriodTime ?? undefined;
    message.rewardAddr = object.rewardAddr ?? "";
    message.whitelistedRelayers = object.whitelistedRelayers?.map((e) => e) || [];
    message.dishonor = object.dishonor ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
