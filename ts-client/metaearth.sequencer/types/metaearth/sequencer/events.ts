// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: metaearth/sequencer/events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Coin } from "../../cosmos/base/v1beta1/coin";

export const protobufPackage = "metaearth.sequencer";

/** EventIncreasedBond is an event emitted when a sequencer's bond is increased. */
export interface EventIncreasedBond {
  /** sequencer is the bech32-encoded address of the sequencer */
  sequencer: string;
  /** added_amount is the amount of coins added to the sequencer's bond */
  addedAmount:
    | Coin
    | undefined;
  /** bond is the new active bond amount of the sequencer */
  bond: Coin[];
}

export interface EventUpdateRewardAddress {
  /** Operator is the bech32-encoded address of the actor sending the update */
  creator: string;
  /** RewardAddr is a bech32 encoded sdk acc address */
  rewardAddr: string;
}

export interface EventUpdateWhitelistedRelayers {
  /** Operator is the bech32-encoded address of the actor sending the update */
  creator: string;
  /**
   * Relayers is an array of the whitelisted relayer addresses. Addresses are
   * bech32-encoded strings.
   */
  relayers: string[];
}

/** On a sequencer kicking the incumbent proposer */
export interface EventKickedProposer {
  rollapp: string;
  /**
   * Kicker is the bech32-encoded address of the sequencer who triggered the
   * kick
   */
  kicker: string;
  /** Proposer is the bech32-encoded address of the proposer who was kicked */
  proposer: string;
}

/** Whenever the proposer changes to a new proposer */
export interface EventProposerChange {
  rollapp: string;
  /** Before is the bech32-encoded address of the old proposer */
  before: string;
  /** After is the bech32-encoded address of the new proposer */
  after: string;
}

/** When a sequencer opt-in status changes */
export interface EventOptInStatusChange {
  rollapp: string;
  /** Sequencer is the bech32-encoded address of the old proposer */
  sequencer: string;
  before: boolean;
  after: boolean;
}

function createBaseEventIncreasedBond(): EventIncreasedBond {
  return { sequencer: "", addedAmount: undefined, bond: [] };
}

export const EventIncreasedBond: MessageFns<EventIncreasedBond> = {
  encode(message: EventIncreasedBond, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sequencer !== "") {
      writer.uint32(10).string(message.sequencer);
    }
    if (message.addedAmount !== undefined) {
      Coin.encode(message.addedAmount, writer.uint32(18).fork()).join();
    }
    for (const v of message.bond) {
      Coin.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventIncreasedBond {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventIncreasedBond();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sequencer = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.addedAmount = Coin.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.bond.push(Coin.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventIncreasedBond {
    return {
      sequencer: isSet(object.sequencer) ? globalThis.String(object.sequencer) : "",
      addedAmount: isSet(object.addedAmount)
        ? Coin.fromJSON(object.addedAmount)
        : isSet(object.added_amount)
        ? Coin.fromJSON(object.added_amount)
        : undefined,
      bond: globalThis.Array.isArray(object?.bond) ? object.bond.map((e: any) => Coin.fromJSON(e)) : [],
    };
  },

  toJSON(message: EventIncreasedBond): unknown {
    const obj: any = {};
    if (message.sequencer !== "") {
      obj.sequencer = message.sequencer;
    }
    if (message.addedAmount !== undefined) {
      obj.addedAmount = Coin.toJSON(message.addedAmount);
    }
    if (message.bond?.length) {
      obj.bond = message.bond.map((e) => Coin.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventIncreasedBond>, I>>(base?: I): EventIncreasedBond {
    return EventIncreasedBond.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventIncreasedBond>, I>>(object: I): EventIncreasedBond {
    const message = createBaseEventIncreasedBond();
    message.sequencer = object.sequencer ?? "";
    message.addedAmount = (object.addedAmount !== undefined && object.addedAmount !== null)
      ? Coin.fromPartial(object.addedAmount)
      : undefined;
    message.bond = object.bond?.map((e) => Coin.fromPartial(e)) || [];
    return message;
  },
};

function createBaseEventUpdateRewardAddress(): EventUpdateRewardAddress {
  return { creator: "", rewardAddr: "" };
}

export const EventUpdateRewardAddress: MessageFns<EventUpdateRewardAddress> = {
  encode(message: EventUpdateRewardAddress, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.rewardAddr !== "") {
      writer.uint32(18).string(message.rewardAddr);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventUpdateRewardAddress {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventUpdateRewardAddress();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.creator = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.rewardAddr = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventUpdateRewardAddress {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      rewardAddr: isSet(object.rewardAddr)
        ? globalThis.String(object.rewardAddr)
        : isSet(object.reward_addr)
        ? globalThis.String(object.reward_addr)
        : "",
    };
  },

  toJSON(message: EventUpdateRewardAddress): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.rewardAddr !== "") {
      obj.rewardAddr = message.rewardAddr;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventUpdateRewardAddress>, I>>(base?: I): EventUpdateRewardAddress {
    return EventUpdateRewardAddress.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventUpdateRewardAddress>, I>>(object: I): EventUpdateRewardAddress {
    const message = createBaseEventUpdateRewardAddress();
    message.creator = object.creator ?? "";
    message.rewardAddr = object.rewardAddr ?? "";
    return message;
  },
};

function createBaseEventUpdateWhitelistedRelayers(): EventUpdateWhitelistedRelayers {
  return { creator: "", relayers: [] };
}

export const EventUpdateWhitelistedRelayers: MessageFns<EventUpdateWhitelistedRelayers> = {
  encode(message: EventUpdateWhitelistedRelayers, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    for (const v of message.relayers) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventUpdateWhitelistedRelayers {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventUpdateWhitelistedRelayers();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.creator = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.relayers.push(reader.string());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventUpdateWhitelistedRelayers {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      relayers: globalThis.Array.isArray(object?.relayers) ? object.relayers.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: EventUpdateWhitelistedRelayers): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.relayers?.length) {
      obj.relayers = message.relayers;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventUpdateWhitelistedRelayers>, I>>(base?: I): EventUpdateWhitelistedRelayers {
    return EventUpdateWhitelistedRelayers.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventUpdateWhitelistedRelayers>, I>>(
    object: I,
  ): EventUpdateWhitelistedRelayers {
    const message = createBaseEventUpdateWhitelistedRelayers();
    message.creator = object.creator ?? "";
    message.relayers = object.relayers?.map((e) => e) || [];
    return message;
  },
};

function createBaseEventKickedProposer(): EventKickedProposer {
  return { rollapp: "", kicker: "", proposer: "" };
}

export const EventKickedProposer: MessageFns<EventKickedProposer> = {
  encode(message: EventKickedProposer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollapp !== "") {
      writer.uint32(26).string(message.rollapp);
    }
    if (message.kicker !== "") {
      writer.uint32(10).string(message.kicker);
    }
    if (message.proposer !== "") {
      writer.uint32(18).string(message.proposer);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventKickedProposer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventKickedProposer();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.rollapp = reader.string();
            continue;
          }
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.kicker = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.proposer = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventKickedProposer {
    return {
      rollapp: isSet(object.rollapp) ? globalThis.String(object.rollapp) : "",
      kicker: isSet(object.kicker) ? globalThis.String(object.kicker) : "",
      proposer: isSet(object.proposer) ? globalThis.String(object.proposer) : "",
    };
  },

  toJSON(message: EventKickedProposer): unknown {
    const obj: any = {};
    if (message.rollapp !== "") {
      obj.rollapp = message.rollapp;
    }
    if (message.kicker !== "") {
      obj.kicker = message.kicker;
    }
    if (message.proposer !== "") {
      obj.proposer = message.proposer;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventKickedProposer>, I>>(base?: I): EventKickedProposer {
    return EventKickedProposer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventKickedProposer>, I>>(object: I): EventKickedProposer {
    const message = createBaseEventKickedProposer();
    message.rollapp = object.rollapp ?? "";
    message.kicker = object.kicker ?? "";
    message.proposer = object.proposer ?? "";
    return message;
  },
};

function createBaseEventProposerChange(): EventProposerChange {
  return { rollapp: "", before: "", after: "" };
}

export const EventProposerChange: MessageFns<EventProposerChange> = {
  encode(message: EventProposerChange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollapp !== "") {
      writer.uint32(26).string(message.rollapp);
    }
    if (message.before !== "") {
      writer.uint32(10).string(message.before);
    }
    if (message.after !== "") {
      writer.uint32(18).string(message.after);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventProposerChange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventProposerChange();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.rollapp = reader.string();
            continue;
          }
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.before = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.after = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventProposerChange {
    return {
      rollapp: isSet(object.rollapp) ? globalThis.String(object.rollapp) : "",
      before: isSet(object.before) ? globalThis.String(object.before) : "",
      after: isSet(object.after) ? globalThis.String(object.after) : "",
    };
  },

  toJSON(message: EventProposerChange): unknown {
    const obj: any = {};
    if (message.rollapp !== "") {
      obj.rollapp = message.rollapp;
    }
    if (message.before !== "") {
      obj.before = message.before;
    }
    if (message.after !== "") {
      obj.after = message.after;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventProposerChange>, I>>(base?: I): EventProposerChange {
    return EventProposerChange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventProposerChange>, I>>(object: I): EventProposerChange {
    const message = createBaseEventProposerChange();
    message.rollapp = object.rollapp ?? "";
    message.before = object.before ?? "";
    message.after = object.after ?? "";
    return message;
  },
};

function createBaseEventOptInStatusChange(): EventOptInStatusChange {
  return { rollapp: "", sequencer: "", before: false, after: false };
}

export const EventOptInStatusChange: MessageFns<EventOptInStatusChange> = {
  encode(message: EventOptInStatusChange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollapp !== "") {
      writer.uint32(26).string(message.rollapp);
    }
    if (message.sequencer !== "") {
      writer.uint32(10).string(message.sequencer);
    }
    if (message.before !== false) {
      writer.uint32(16).bool(message.before);
    }
    if (message.after !== false) {
      writer.uint32(32).bool(message.after);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventOptInStatusChange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventOptInStatusChange();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.rollapp = reader.string();
            continue;
          }
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sequencer = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.before = reader.bool();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.after = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventOptInStatusChange {
    return {
      rollapp: isSet(object.rollapp) ? globalThis.String(object.rollapp) : "",
      sequencer: isSet(object.sequencer) ? globalThis.String(object.sequencer) : "",
      before: isSet(object.before) ? globalThis.Boolean(object.before) : false,
      after: isSet(object.after) ? globalThis.Boolean(object.after) : false,
    };
  },

  toJSON(message: EventOptInStatusChange): unknown {
    const obj: any = {};
    if (message.rollapp !== "") {
      obj.rollapp = message.rollapp;
    }
    if (message.sequencer !== "") {
      obj.sequencer = message.sequencer;
    }
    if (message.before !== false) {
      obj.before = message.before;
    }
    if (message.after !== false) {
      obj.after = message.after;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventOptInStatusChange>, I>>(base?: I): EventOptInStatusChange {
    return EventOptInStatusChange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventOptInStatusChange>, I>>(object: I): EventOptInStatusChange {
    const message = createBaseEventOptInStatusChange();
    message.rollapp = object.rollapp ?? "";
    message.sequencer = object.sequencer ?? "";
    message.before = object.before ?? false;
    message.after = object.after ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
