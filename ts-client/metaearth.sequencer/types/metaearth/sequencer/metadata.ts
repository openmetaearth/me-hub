// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: metaearth/sequencer/metadata.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "metaearth.sequencer";

/** Metadata defines rollapp/sequencer extra information. */
export interface SequencerMetadata {
  /** moniker defines a human-readable name for the sequencer. */
  moniker: string;
  /** details define other optional details. */
  details: string;
  /** bootstrap nodes list */
  p2pSeeds: string[];
  /** RPCs list */
  rpcs: string[];
  /** evm RPCs list */
  evmRpcs: string[];
  /** REST API URLs */
  restApiUrls: string[];
  /** block explorer URL */
  explorerUrl: string;
  /** genesis URLs */
  genesisUrls: string[];
  /** contact details */
  contactDetails:
    | ContactDetails
    | undefined;
  /** json dump the sequencer can add (limited by size) */
  extraData: Uint8Array;
  /** snapshots of the sequencer */
  snapshots: SnapshotInfo[];
  /** gas_price defines the value for each gas unit */
  gasPrice: string;
  /** fee_denom is the base denom for fees */
  feeDenom: DenomMetadata | undefined;
}

export interface ContactDetails {
  /** website URL */
  website: string;
  /** telegram link */
  telegram: string;
  /** twitter link */
  x: string;
}

export interface SnapshotInfo {
  /** the snapshot url */
  snapshotUrl: string;
  /** The snapshot height */
  height: number;
  /** sha-256 checksum value for the snapshot file */
  checksum: string;
}

export interface DenomMetadata {
  display: string;
  base: string;
  exponent: number;
}

function createBaseSequencerMetadata(): SequencerMetadata {
  return {
    moniker: "",
    details: "",
    p2pSeeds: [],
    rpcs: [],
    evmRpcs: [],
    restApiUrls: [],
    explorerUrl: "",
    genesisUrls: [],
    contactDetails: undefined,
    extraData: new Uint8Array(0),
    snapshots: [],
    gasPrice: "",
    feeDenom: undefined,
  };
}

export const SequencerMetadata: MessageFns<SequencerMetadata> = {
  encode(message: SequencerMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.moniker !== "") {
      writer.uint32(10).string(message.moniker);
    }
    if (message.details !== "") {
      writer.uint32(42).string(message.details);
    }
    for (const v of message.p2pSeeds) {
      writer.uint32(50).string(v!);
    }
    for (const v of message.rpcs) {
      writer.uint32(58).string(v!);
    }
    for (const v of message.evmRpcs) {
      writer.uint32(66).string(v!);
    }
    for (const v of message.restApiUrls) {
      writer.uint32(74).string(v!);
    }
    if (message.explorerUrl !== "") {
      writer.uint32(82).string(message.explorerUrl);
    }
    for (const v of message.genesisUrls) {
      writer.uint32(90).string(v!);
    }
    if (message.contactDetails !== undefined) {
      ContactDetails.encode(message.contactDetails, writer.uint32(98).fork()).join();
    }
    if (message.extraData.length !== 0) {
      writer.uint32(106).bytes(message.extraData);
    }
    for (const v of message.snapshots) {
      SnapshotInfo.encode(v!, writer.uint32(114).fork()).join();
    }
    if (message.gasPrice !== "") {
      writer.uint32(122).string(message.gasPrice);
    }
    if (message.feeDenom !== undefined) {
      DenomMetadata.encode(message.feeDenom, writer.uint32(130).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SequencerMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSequencerMetadata();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.moniker = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.details = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.p2pSeeds.push(reader.string());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.rpcs.push(reader.string());
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.evmRpcs.push(reader.string());
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.restApiUrls.push(reader.string());
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.explorerUrl = reader.string();
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.genesisUrls.push(reader.string());
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.contactDetails = ContactDetails.decode(reader, reader.uint32());
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.extraData = reader.bytes();
            continue;
          }
          case 14: {
            if (tag !== 114) {
              break;
            }

            message.snapshots.push(SnapshotInfo.decode(reader, reader.uint32()));
            continue;
          }
          case 15: {
            if (tag !== 122) {
              break;
            }

            message.gasPrice = reader.string();
            continue;
          }
          case 16: {
            if (tag !== 130) {
              break;
            }

            message.feeDenom = DenomMetadata.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SequencerMetadata {
    return {
      moniker: isSet(object.moniker) ? globalThis.String(object.moniker) : "",
      details: isSet(object.details) ? globalThis.String(object.details) : "",
      p2pSeeds: globalThis.Array.isArray(object?.p2pSeeds)
        ? object.p2pSeeds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.p2p_seeds)
        ? object.p2p_seeds.map((e: any) => globalThis.String(e))
        : [],
      rpcs: globalThis.Array.isArray(object?.rpcs) ? object.rpcs.map((e: any) => globalThis.String(e)) : [],
      evmRpcs: globalThis.Array.isArray(object?.evmRpcs)
        ? object.evmRpcs.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.evm_rpcs)
        ? object.evm_rpcs.map((e: any) => globalThis.String(e))
        : [],
      restApiUrls: globalThis.Array.isArray(object?.restApiUrls)
        ? object.restApiUrls.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.rest_api_urls)
        ? object.rest_api_urls.map((e: any) => globalThis.String(e))
        : [],
      explorerUrl: isSet(object.explorerUrl)
        ? globalThis.String(object.explorerUrl)
        : isSet(object.explorer_url)
        ? globalThis.String(object.explorer_url)
        : "",
      genesisUrls: globalThis.Array.isArray(object?.genesisUrls)
        ? object.genesisUrls.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.genesis_urls)
        ? object.genesis_urls.map((e: any) => globalThis.String(e))
        : [],
      contactDetails: isSet(object.contactDetails)
        ? ContactDetails.fromJSON(object.contactDetails)
        : isSet(object.contact_details)
        ? ContactDetails.fromJSON(object.contact_details)
        : undefined,
      extraData: isSet(object.extraData)
        ? bytesFromBase64(object.extraData)
        : isSet(object.extra_data)
        ? bytesFromBase64(object.extra_data)
        : new Uint8Array(0),
      snapshots: globalThis.Array.isArray(object?.snapshots)
        ? object.snapshots.map((e: any) => SnapshotInfo.fromJSON(e))
        : [],
      gasPrice: isSet(object.gasPrice)
        ? globalThis.String(object.gasPrice)
        : isSet(object.gas_price)
        ? globalThis.String(object.gas_price)
        : "",
      feeDenom: isSet(object.feeDenom)
        ? DenomMetadata.fromJSON(object.feeDenom)
        : isSet(object.fee_denom)
        ? DenomMetadata.fromJSON(object.fee_denom)
        : undefined,
    };
  },

  toJSON(message: SequencerMetadata): unknown {
    const obj: any = {};
    if (message.moniker !== "") {
      obj.moniker = message.moniker;
    }
    if (message.details !== "") {
      obj.details = message.details;
    }
    if (message.p2pSeeds?.length) {
      obj.p2pSeeds = message.p2pSeeds;
    }
    if (message.rpcs?.length) {
      obj.rpcs = message.rpcs;
    }
    if (message.evmRpcs?.length) {
      obj.evmRpcs = message.evmRpcs;
    }
    if (message.restApiUrls?.length) {
      obj.restApiUrls = message.restApiUrls;
    }
    if (message.explorerUrl !== "") {
      obj.explorerUrl = message.explorerUrl;
    }
    if (message.genesisUrls?.length) {
      obj.genesisUrls = message.genesisUrls;
    }
    if (message.contactDetails !== undefined) {
      obj.contactDetails = ContactDetails.toJSON(message.contactDetails);
    }
    if (message.extraData.length !== 0) {
      obj.extraData = base64FromBytes(message.extraData);
    }
    if (message.snapshots?.length) {
      obj.snapshots = message.snapshots.map((e) => SnapshotInfo.toJSON(e));
    }
    if (message.gasPrice !== "") {
      obj.gasPrice = message.gasPrice;
    }
    if (message.feeDenom !== undefined) {
      obj.feeDenom = DenomMetadata.toJSON(message.feeDenom);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SequencerMetadata>, I>>(base?: I): SequencerMetadata {
    return SequencerMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SequencerMetadata>, I>>(object: I): SequencerMetadata {
    const message = createBaseSequencerMetadata();
    message.moniker = object.moniker ?? "";
    message.details = object.details ?? "";
    message.p2pSeeds = object.p2pSeeds?.map((e) => e) || [];
    message.rpcs = object.rpcs?.map((e) => e) || [];
    message.evmRpcs = object.evmRpcs?.map((e) => e) || [];
    message.restApiUrls = object.restApiUrls?.map((e) => e) || [];
    message.explorerUrl = object.explorerUrl ?? "";
    message.genesisUrls = object.genesisUrls?.map((e) => e) || [];
    message.contactDetails = (object.contactDetails !== undefined && object.contactDetails !== null)
      ? ContactDetails.fromPartial(object.contactDetails)
      : undefined;
    message.extraData = object.extraData ?? new Uint8Array(0);
    message.snapshots = object.snapshots?.map((e) => SnapshotInfo.fromPartial(e)) || [];
    message.gasPrice = object.gasPrice ?? "";
    message.feeDenom = (object.feeDenom !== undefined && object.feeDenom !== null)
      ? DenomMetadata.fromPartial(object.feeDenom)
      : undefined;
    return message;
  },
};

function createBaseContactDetails(): ContactDetails {
  return { website: "", telegram: "", x: "" };
}

export const ContactDetails: MessageFns<ContactDetails> = {
  encode(message: ContactDetails, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.website !== "") {
      writer.uint32(90).string(message.website);
    }
    if (message.telegram !== "") {
      writer.uint32(10).string(message.telegram);
    }
    if (message.x !== "") {
      writer.uint32(18).string(message.x);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContactDetails {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseContactDetails();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.website = reader.string();
            continue;
          }
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.telegram = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.x = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ContactDetails {
    return {
      website: isSet(object.website) ? globalThis.String(object.website) : "",
      telegram: isSet(object.telegram) ? globalThis.String(object.telegram) : "",
      x: isSet(object.x) ? globalThis.String(object.x) : "",
    };
  },

  toJSON(message: ContactDetails): unknown {
    const obj: any = {};
    if (message.website !== "") {
      obj.website = message.website;
    }
    if (message.telegram !== "") {
      obj.telegram = message.telegram;
    }
    if (message.x !== "") {
      obj.x = message.x;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ContactDetails>, I>>(base?: I): ContactDetails {
    return ContactDetails.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContactDetails>, I>>(object: I): ContactDetails {
    const message = createBaseContactDetails();
    message.website = object.website ?? "";
    message.telegram = object.telegram ?? "";
    message.x = object.x ?? "";
    return message;
  },
};

function createBaseSnapshotInfo(): SnapshotInfo {
  return { snapshotUrl: "", height: 0, checksum: "" };
}

export const SnapshotInfo: MessageFns<SnapshotInfo> = {
  encode(message: SnapshotInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.snapshotUrl !== "") {
      writer.uint32(10).string(message.snapshotUrl);
    }
    if (message.height !== 0) {
      writer.uint32(16).uint64(message.height);
    }
    if (message.checksum !== "") {
      writer.uint32(26).string(message.checksum);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SnapshotInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSnapshotInfo();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.snapshotUrl = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.height = longToNumber(reader.uint64());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.checksum = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SnapshotInfo {
    return {
      snapshotUrl: isSet(object.snapshotUrl)
        ? globalThis.String(object.snapshotUrl)
        : isSet(object.snapshot_url)
        ? globalThis.String(object.snapshot_url)
        : "",
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      checksum: isSet(object.checksum) ? globalThis.String(object.checksum) : "",
    };
  },

  toJSON(message: SnapshotInfo): unknown {
    const obj: any = {};
    if (message.snapshotUrl !== "") {
      obj.snapshotUrl = message.snapshotUrl;
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.checksum !== "") {
      obj.checksum = message.checksum;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SnapshotInfo>, I>>(base?: I): SnapshotInfo {
    return SnapshotInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SnapshotInfo>, I>>(object: I): SnapshotInfo {
    const message = createBaseSnapshotInfo();
    message.snapshotUrl = object.snapshotUrl ?? "";
    message.height = object.height ?? 0;
    message.checksum = object.checksum ?? "";
    return message;
  },
};

function createBaseDenomMetadata(): DenomMetadata {
  return { display: "", base: "", exponent: 0 };
}

export const DenomMetadata: MessageFns<DenomMetadata> = {
  encode(message: DenomMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.display !== "") {
      writer.uint32(10).string(message.display);
    }
    if (message.base !== "") {
      writer.uint32(18).string(message.base);
    }
    if (message.exponent !== 0) {
      writer.uint32(24).uint32(message.exponent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DenomMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDenomMetadata();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.display = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.base = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.exponent = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DenomMetadata {
    return {
      display: isSet(object.display) ? globalThis.String(object.display) : "",
      base: isSet(object.base) ? globalThis.String(object.base) : "",
      exponent: isSet(object.exponent) ? globalThis.Number(object.exponent) : 0,
    };
  },

  toJSON(message: DenomMetadata): unknown {
    const obj: any = {};
    if (message.display !== "") {
      obj.display = message.display;
    }
    if (message.base !== "") {
      obj.base = message.base;
    }
    if (message.exponent !== 0) {
      obj.exponent = Math.round(message.exponent);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DenomMetadata>, I>>(base?: I): DenomMetadata {
    return DenomMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DenomMetadata>, I>>(object: I): DenomMetadata {
    const message = createBaseDenomMetadata();
    message.display = object.display ?? "";
    message.base = object.base ?? "";
    message.exponent = object.exponent ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
