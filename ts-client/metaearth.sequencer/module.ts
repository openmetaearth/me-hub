// Generated by Ignite ignite.com/cli

import { StdFee } from "@cosmjs/launchpad";
import { SigningStargateClient, DeliverTxResponse } from "@cosmjs/stargate";
import { EncodeObject, GeneratedType, OfflineSigner, Registry } from "@cosmjs/proto-signing";
import { msgTypes } from './registry';
import { IgniteClient } from "../client"
import { MissingWalletError } from "../helpers"
import { Api } from "./rest";
import { MsgCreateSequencer } from "./types/metaearth/sequencer/tx";
import { MsgReplaceProposerRequest } from "./types/metaearth/sequencer/tx";

import { Description as typeDescription} from "./types"
import { Params as typeParams} from "./types"
import { Sequencer as typeSequencer} from "./types"
import { MsgRepalceProposer as typeMsgRepalceProposer} from "./types"
import { MsgStoreReplaceProposer as typeMsgStoreReplaceProposer} from "./types"

export { MsgCreateSequencer, MsgReplaceProposerRequest };

type sendMsgCreateSequencerParams = {
  value: MsgCreateSequencer,
  fee?: StdFee,
  memo?: string
};

type sendMsgReplaceProposerRequestParams = {
  value: MsgReplaceProposerRequest,
  fee?: StdFee,
  memo?: string
};


type msgCreateSequencerParams = {
  value: MsgCreateSequencer,
};

type msgReplaceProposerRequestParams = {
  value: MsgReplaceProposerRequest,
};


export const registry = new Registry(msgTypes);

type Field = {
	name: string;
	type: unknown;
}
function getStructure(template) {
	const structure: {fields: Field[]} = { fields: [] }
	for (let [key, value] of Object.entries(template)) {
		let field = { name: key, type: typeof value }
		structure.fields.push(field)
	}
	return structure
}
const defaultFee = {
  amount: [],
  gas: "200000",
};

interface TxClientOptions {
  addr: string
	prefix: string
	signer?: OfflineSigner
}

export const txClient = ({ signer, prefix, addr }: TxClientOptions = { addr: "http://localhost:26657", prefix: "cosmos" }) => {

  return {
		
		async sendMsgCreateSequencer({ value, fee, memo }: sendMsgCreateSequencerParams): Promise<DeliverTxResponse> {
			if (!signer) {
					throw new Error('TxClient:sendMsgCreateSequencer: Unable to sign Tx. Signer is not present.')
			}
			try {			
				const { address } = (await signer.getAccounts())[0]; 
				const signingClient = await SigningStargateClient.connectWithSigner(addr,signer,{registry, prefix});
				let msg = this.msgCreateSequencer({ value: MsgCreateSequencer.fromPartial(value) })
				return await signingClient.signAndBroadcast(address, [msg], fee ? fee : defaultFee, memo)
			} catch (e: any) {
				throw new Error('TxClient:sendMsgCreateSequencer: Could not broadcast Tx: '+ e.message)
			}
		},
		
		async sendMsgReplaceProposerRequest({ value, fee, memo }: sendMsgReplaceProposerRequestParams): Promise<DeliverTxResponse> {
			if (!signer) {
					throw new Error('TxClient:sendMsgReplaceProposerRequest: Unable to sign Tx. Signer is not present.')
			}
			try {			
				const { address } = (await signer.getAccounts())[0]; 
				const signingClient = await SigningStargateClient.connectWithSigner(addr,signer,{registry, prefix});
				let msg = this.msgReplaceProposerRequest({ value: MsgReplaceProposerRequest.fromPartial(value) })
				return await signingClient.signAndBroadcast(address, [msg], fee ? fee : defaultFee, memo)
			} catch (e: any) {
				throw new Error('TxClient:sendMsgReplaceProposerRequest: Could not broadcast Tx: '+ e.message)
			}
		},
		
		
		msgCreateSequencer({ value }: msgCreateSequencerParams): EncodeObject {
			try {
				return { typeUrl: "/metaearth.sequencer.MsgCreateSequencer", value: MsgCreateSequencer.fromPartial( value ) }  
			} catch (e: any) {
				throw new Error('TxClient:MsgCreateSequencer: Could not create message: ' + e.message)
			}
		},
		
		msgReplaceProposerRequest({ value }: msgReplaceProposerRequestParams): EncodeObject {
			try {
				return { typeUrl: "/metaearth.sequencer.MsgReplaceProposerRequest", value: MsgReplaceProposerRequest.fromPartial( value ) }  
			} catch (e: any) {
				throw new Error('TxClient:MsgReplaceProposerRequest: Could not create message: ' + e.message)
			}
		},
		
	}
};

interface QueryClientOptions {
  addr: string
}

export const queryClient = ({ addr: addr }: QueryClientOptions = { addr: "http://localhost:1317" }) => {
  return new Api({ baseURL: addr });
};

class SDKModule {
	public query: ReturnType<typeof queryClient>;
	public tx: ReturnType<typeof txClient>;
	public structure: Record<string,unknown>;
	public registry: Array<[string, GeneratedType]> = [];

	constructor(client: IgniteClient) {		
	
		this.query = queryClient({ addr: client.env.apiURL });		
		this.updateTX(client);
		this.structure =  {
						Description: getStructure(typeDescription.fromPartial({})),
						Params: getStructure(typeParams.fromPartial({})),
						Sequencer: getStructure(typeSequencer.fromPartial({})),
						MsgRepalceProposer: getStructure(typeMsgRepalceProposer.fromPartial({})),
						MsgStoreReplaceProposer: getStructure(typeMsgStoreReplaceProposer.fromPartial({})),
						
		};
		client.on('signer-changed',(signer) => {			
		 this.updateTX(client);
		})
	}
	updateTX(client: IgniteClient) {
    const methods = txClient({
        signer: client.signer,
        addr: client.env.rpcURL,
        prefix: client.env.prefix ?? "cosmos",
    })
	
    this.tx = methods;
    for (let m in methods) {
        this.tx[m] = methods[m].bind(this.tx);
    }
	}
};

const Module = (test: IgniteClient) => {
	return {
		module: {
			MetaearthSequencer: new SDKModule(test)
		},
		registry: msgTypes
  }
}
export default Module;