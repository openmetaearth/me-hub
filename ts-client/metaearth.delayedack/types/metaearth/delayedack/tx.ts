// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: metaearth/delayedack/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { RollappPacket_Type, rollappPacket_TypeFromJSON, rollappPacket_TypeToJSON } from "../common/rollapp_packet";
import { Params } from "./params";

export const protobufPackage = "metaearth.delayedack";

/** MsgUpdateParams allows to update module params. */
export interface MsgUpdateParams {
  /** Authority is the address that controls the module. */
  authority: string;
  /** NewParams should be fully populated. */
  params: Params | undefined;
}

export interface MsgUpdateParamsResponse {
}

/** MsgFinalizePacket finalizes a single packet. */
export interface MsgFinalizePacket {
  /** Sender is the signer of the message. */
  sender: string;
  /** RollappID is the ID of the rollapp. */
  rollappId: string;
  /** PacketProofHeight is a height at which the proof was retrieved. */
  packetProofHeight: number;
  /** PacketType is a type of the packet. Eg, RECV, ACK, TIMEOUT. */
  packetType: RollappPacket_Type;
  /** PacketSrcChannel identifies the channel end on the sending chain. */
  packetSrcChannel: string;
  /** PacketSequence is a sequence number of the packet. */
  packetSequence: number;
}

export interface MsgFinalizePacketResponse {
}

/** MsgFinalizePacketByPacketKey finalizes a single packet by the packet key. */
export interface MsgFinalizePacketByPacketKey {
  /** Sender is the signer of the message. */
  sender: string;
  /** PacketKey is a key of the packet. */
  packetKey: string;
}

export interface MsgFinalizePacketByPacketKeyResponse {
}

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams: MessageFns<MsgUpdateParams> = {
  encode(message: MsgUpdateParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgUpdateParams();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.authority = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.params = Params.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(base?: I): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(object: I): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse: MessageFns<MsgUpdateParamsResponse> = {
  encode(_: MsgUpdateParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgUpdateParamsResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(base?: I): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(_: I): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

function createBaseMsgFinalizePacket(): MsgFinalizePacket {
  return { sender: "", rollappId: "", packetProofHeight: 0, packetType: 0, packetSrcChannel: "", packetSequence: 0 };
}

export const MsgFinalizePacket: MessageFns<MsgFinalizePacket> = {
  encode(message: MsgFinalizePacket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.rollappId !== "") {
      writer.uint32(18).string(message.rollappId);
    }
    if (message.packetProofHeight !== 0) {
      writer.uint32(24).uint64(message.packetProofHeight);
    }
    if (message.packetType !== 0) {
      writer.uint32(32).int32(message.packetType);
    }
    if (message.packetSrcChannel !== "") {
      writer.uint32(42).string(message.packetSrcChannel);
    }
    if (message.packetSequence !== 0) {
      writer.uint32(48).uint64(message.packetSequence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgFinalizePacket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgFinalizePacket();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sender = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.rollappId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.packetProofHeight = longToNumber(reader.uint64());
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.packetType = reader.int32() as any;
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.packetSrcChannel = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.packetSequence = longToNumber(reader.uint64());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MsgFinalizePacket {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      rollappId: isSet(object.rollappId)
        ? globalThis.String(object.rollappId)
        : isSet(object.rollapp_id)
        ? globalThis.String(object.rollapp_id)
        : "",
      packetProofHeight: isSet(object.packetProofHeight)
        ? globalThis.Number(object.packetProofHeight)
        : isSet(object.packet_proof_height)
        ? globalThis.Number(object.packet_proof_height)
        : 0,
      packetType: isSet(object.packetType)
        ? rollappPacket_TypeFromJSON(object.packetType)
        : isSet(object.packet_type)
        ? rollappPacket_TypeFromJSON(object.packet_type)
        : 0,
      packetSrcChannel: isSet(object.packetSrcChannel)
        ? globalThis.String(object.packetSrcChannel)
        : isSet(object.packet_src_channel)
        ? globalThis.String(object.packet_src_channel)
        : "",
      packetSequence: isSet(object.packetSequence)
        ? globalThis.Number(object.packetSequence)
        : isSet(object.packet_sequence)
        ? globalThis.Number(object.packet_sequence)
        : 0,
    };
  },

  toJSON(message: MsgFinalizePacket): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.rollappId !== "") {
      obj.rollappId = message.rollappId;
    }
    if (message.packetProofHeight !== 0) {
      obj.packetProofHeight = Math.round(message.packetProofHeight);
    }
    if (message.packetType !== 0) {
      obj.packetType = rollappPacket_TypeToJSON(message.packetType);
    }
    if (message.packetSrcChannel !== "") {
      obj.packetSrcChannel = message.packetSrcChannel;
    }
    if (message.packetSequence !== 0) {
      obj.packetSequence = Math.round(message.packetSequence);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgFinalizePacket>, I>>(base?: I): MsgFinalizePacket {
    return MsgFinalizePacket.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgFinalizePacket>, I>>(object: I): MsgFinalizePacket {
    const message = createBaseMsgFinalizePacket();
    message.sender = object.sender ?? "";
    message.rollappId = object.rollappId ?? "";
    message.packetProofHeight = object.packetProofHeight ?? 0;
    message.packetType = object.packetType ?? 0;
    message.packetSrcChannel = object.packetSrcChannel ?? "";
    message.packetSequence = object.packetSequence ?? 0;
    return message;
  },
};

function createBaseMsgFinalizePacketResponse(): MsgFinalizePacketResponse {
  return {};
}

export const MsgFinalizePacketResponse: MessageFns<MsgFinalizePacketResponse> = {
  encode(_: MsgFinalizePacketResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgFinalizePacketResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgFinalizePacketResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): MsgFinalizePacketResponse {
    return {};
  },

  toJSON(_: MsgFinalizePacketResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgFinalizePacketResponse>, I>>(base?: I): MsgFinalizePacketResponse {
    return MsgFinalizePacketResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgFinalizePacketResponse>, I>>(_: I): MsgFinalizePacketResponse {
    const message = createBaseMsgFinalizePacketResponse();
    return message;
  },
};

function createBaseMsgFinalizePacketByPacketKey(): MsgFinalizePacketByPacketKey {
  return { sender: "", packetKey: "" };
}

export const MsgFinalizePacketByPacketKey: MessageFns<MsgFinalizePacketByPacketKey> = {
  encode(message: MsgFinalizePacketByPacketKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.packetKey !== "") {
      writer.uint32(18).string(message.packetKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgFinalizePacketByPacketKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgFinalizePacketByPacketKey();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sender = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.packetKey = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MsgFinalizePacketByPacketKey {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      packetKey: isSet(object.packetKey)
        ? globalThis.String(object.packetKey)
        : isSet(object.packet_key)
        ? globalThis.String(object.packet_key)
        : "",
    };
  },

  toJSON(message: MsgFinalizePacketByPacketKey): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.packetKey !== "") {
      obj.packetKey = message.packetKey;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgFinalizePacketByPacketKey>, I>>(base?: I): MsgFinalizePacketByPacketKey {
    return MsgFinalizePacketByPacketKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgFinalizePacketByPacketKey>, I>>(object: I): MsgFinalizePacketByPacketKey {
    const message = createBaseMsgFinalizePacketByPacketKey();
    message.sender = object.sender ?? "";
    message.packetKey = object.packetKey ?? "";
    return message;
  },
};

function createBaseMsgFinalizePacketByPacketKeyResponse(): MsgFinalizePacketByPacketKeyResponse {
  return {};
}

export const MsgFinalizePacketByPacketKeyResponse: MessageFns<MsgFinalizePacketByPacketKeyResponse> = {
  encode(_: MsgFinalizePacketByPacketKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgFinalizePacketByPacketKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgFinalizePacketByPacketKeyResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): MsgFinalizePacketByPacketKeyResponse {
    return {};
  },

  toJSON(_: MsgFinalizePacketByPacketKeyResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgFinalizePacketByPacketKeyResponse>, I>>(
    base?: I,
  ): MsgFinalizePacketByPacketKeyResponse {
    return MsgFinalizePacketByPacketKeyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgFinalizePacketByPacketKeyResponse>, I>>(
    _: I,
  ): MsgFinalizePacketByPacketKeyResponse {
    const message = createBaseMsgFinalizePacketByPacketKeyResponse();
    return message;
  },
};

/** Msg defines the Msg service. */
export interface Msg {
  /** UpdateParams is used for updating module params. */
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse>;
  /** FinalizePacket finalizes a single packet. */
  FinalizePacket(request: MsgFinalizePacket): Promise<MsgFinalizePacketResponse>;
  FinalizePacketByPacketKey(request: MsgFinalizePacketByPacketKey): Promise<MsgFinalizePacketByPacketKeyResponse>;
}

export const MsgServiceName = "metaearth.delayedack.Msg";
export class MsgClientImpl implements Msg {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || MsgServiceName;
    this.rpc = rpc;
    this.UpdateParams = this.UpdateParams.bind(this);
    this.FinalizePacket = this.FinalizePacket.bind(this);
    this.FinalizePacketByPacketKey = this.FinalizePacketByPacketKey.bind(this);
  }
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse> {
    const data = MsgUpdateParams.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateParams", data);
    return promise.then((data) => MsgUpdateParamsResponse.decode(new BinaryReader(data)));
  }

  FinalizePacket(request: MsgFinalizePacket): Promise<MsgFinalizePacketResponse> {
    const data = MsgFinalizePacket.encode(request).finish();
    const promise = this.rpc.request(this.service, "FinalizePacket", data);
    return promise.then((data) => MsgFinalizePacketResponse.decode(new BinaryReader(data)));
  }

  FinalizePacketByPacketKey(request: MsgFinalizePacketByPacketKey): Promise<MsgFinalizePacketByPacketKeyResponse> {
    const data = MsgFinalizePacketByPacketKey.encode(request).finish();
    const promise = this.rpc.request(this.service, "FinalizePacketByPacketKey", data);
    return promise.then((data) => MsgFinalizePacketByPacketKeyResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
